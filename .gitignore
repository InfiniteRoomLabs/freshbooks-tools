# build / test artifacts
dist/
coverage*.out
*.test
# worktrees for parallel phase batches
.worktrees/
# agent directories
.claude/
.codex/
.gemini/
.kittify/
# local env / secrets (never commit)
.env
.envrc
credentials.json

# local secret wiring (vault item names are operator-specific)
fnox.toml
mise.local.toml

# docs site (Docusaurus): site/docs/ is generated by scripts/site-sync.sh,
# never hand-edited or committed; node_modules/build/cache are the usual
# JS build artifacts
site/node_modules/
site/docs/
site/build/
site/.docusaurus/
