# GOAL.md -- autonomous next-phase goal

Run with `/goal complete everything in @GOAL.md`. Each run ships exactly one phase of `freshbooks-tools` through the pipeline: verify premise -> implement -> four-lane review gate -> merge -> ship -> retarget this file. The design oracle is `docs/superpowers/specs/2026-08-22-freshbooks-tools-design.md` (the "spec"); `CLAUDE.md` holds the conventions and dispatch rules; `docs/progress.md` is where we are.

**Shipped so far:** Phase 0 (scaffold) -- `go.work` + three modules, inventory tool + parity check, `mise` gate, CI/release, public repo. Phase 1 (lib core) -- client/transport/errors/page/types, `auth/`, Identity, live-verified OAuth facts. Phase 2 (lib resources, 4 batches) -- all 213 inventory keys, 36 services, coverage ~91.8%. Phase 3 (MCP server) -- 168 tools over 212 keys, stdio + stateless streamable HTTP, `docs/mcp.md`. Phase 4 (CLI) -- 168 registry commands (+`--all`), loopback PKCE login, contexts, `json|yaml|table|name`, `--dry-run`, `api`, exit codes, generated `docs/cli.md`; coverage 91.6%. Phase 5 (release hardening) -- goreleaser builds + `gh` publishes on the prefixed tags (the workflow as scaffolded could not ship one), SHA-pinned CI, syft SBOMs, `docsgen` build tag, `version` fallback, docs pass, 0.1.0 changelog sections, attended tag runbook in `docs/progress.md`. The definitive lib-method table is `docs/phases/3/tools.md`; the command table is `docs/phases/4/commands.md`. Next default target: **the attended v0.1.0 tag step** (stops for Wes at every tag push), then the live-conformance pass. Full roadmap in *Retarget* at the bottom.

---

## Goal (current target: the attended v0.1.0 tag step)

> **Goal -- ship `freshbooks/v0.1.0`, `mcp/v0.1.0`, `cli/v0.1.0`, stopping for Wes at every tag push:**
>
> 1. **Verify the premise before touching anything.** Read `docs/progress.md` ("Next action" is the runbook; follow it step by step, it is the oracle for this run), then `docs/building.md` (release flow) and `.github/workflows/release.yml`. Confirm `main` is green on GitHub, `git status --porcelain` is empty, `gh auth status` has `repo` + `workflow`, `mise install` is current. Re-verify the release premise cheaply: `gh api repos/goreleaser/goreleaser/releases/latest` (a newer goreleaser is fine to leave alone; note it), and the runbook's step-0 scratch-clone dry run must still produce six `freshbooks-mcp_0.1.0_*` archives + `checksums.txt` + six `.sbom.json`. Do the repository-settings pre-flight (tag ruleset, backlog item 10) only with Wes's explicit yes -- it changes GitHub settings.
> 2. **No implementer this run: the lead executes the runbook itself.** No branch; every commit lands on `main` directly (`release(freshbooks): v0.1.0`, `release(mcp,cli): require freshbooks v0.1.0 and cut 0.1.0`, `docs: ship v0.1.0`), each with root `CHANGELOG.md` staged, each pushed and CI-green before the next step. Prepare each tag command in full, present it to Wes as one line, and **wait** -- the tag push is his. After each push: `gh run watch`, `gh release view`, the checksum and `go install ...@v0.1.0` verifications the runbook lists (`freshbooks-mcp version` prints `freshbooks-mcp v0.1.0`; `freshbooks version` prints `v0.1.0` bare).
> 3. **Gate:** CI on `main` (the Release workflow re-runs `ci.yml` before publishing) is the gate; no review lanes. If a Release run goes red after a tag exists, fix on `main` and follow the runbook's re-tag rule (delete and re-push only if nothing was published; otherwise cut `v0.1.1`).
> 4. **Ship** -- README Status column to the released versions, flip the three "once the v0.1.0 tags ship" caveats, `docs/progress.md` ledger row 6 SHIPPED with the three tag shas, root `CHANGELOG.md`, then **retarget THIS file** per *Self-advance* to the live-conformance pass (attended, needs FreshBooks credentials via `fnox exec`). Commit with the docs.
>
> **Done when:** all three GitHub releases exist (`mcp` and `cli` with six archives, `checksums.txt`, six SBOMs; `freshbooks` with notes only), `go install` of both binaries at `@v0.1.0` works outside the workspace and prints the right version, `main` is green, the docs are flipped, **and `GOAL.md` is retargeted to the live-conformance pass.** **Stop** at every tag push (attended by design), for the repository-settings change, and for anything the runbook does not cover.

### Self-advance (do this as the last Ship step, every run)

Leave `GOAL.md` pointed at the **next** phase so the following `/goal` run is paste-ready:
- Update the **"Shipped so far"** line and strike the shipped row in the **Retarget table** (mark it `SHIPPED`, add the merge sha and one-line lesson).
- Rewrite the **`## Goal` heading + block** for the next phase: stage 1 is always "verify the premise" against the spec sections that phase depends on; stage 2 names the branch, the implementer model (from the Retarget table), the work-order template, and the concrete deliverables with acceptance criteria; stage 3 is the gate with the reviewer model one tier above the implementer; stage 4 is ship + retarget.
- Append anything you learned to **Lessons** (process traps only, not feature notes -- those go in `docs/progress.md` and the spec callouts).
- **Next default after the tag step: the live-conformance pass** (attended, needs FreshBooks credentials through `fnox exec`): branch `phase-7/live`, the `live` build-tag suite against a real account, every docs-only `STATE AS OF` fact upgraded to CONFIRMED or corrected with a capture, the MCP and the CLI end to end with a real token. Runbook in `docs/progress.md`.

---

## Lessons from prior runs (read before starting)

Seeded from `hoyle-re` (the reference implementation of this loop). Append, never rewrite.

- **Verify the premise before implementing.** Two of three hoyle-re autonomous runs found the goal's own premise wrong. Here the premise is the spec's reading of the FreshBooks API (section 3) and the Postman inventory; the API's actual behaviour wins over the spec, and the spec gets a `STATE AS OF` callout in the same run.
- **Model tiers are pinned in agent definitions, not inherited.** Omitting `model:` on an `Agent` call uses whatever the agent file pins (several agent-ops agents pin haiku). Pass `model:` explicitly on every dispatch, and keep reviewers one tier above the implementer (sonnet -> opus, opus -> fable). Never use haiku for anything that could hit a permission prompt -- it has no auto mode and stalls the run.
- **Check the agent's `tools:` list before writing its prompt.** Most agent-ops review agents lack `Bash` or `SendMessage` and will appear to end silently. `general-purpose` (tools `*`) works for every lane.
- **Tell agents HOW to deliver.** A named background agent's final text does not reach the lead by itself. Instruct it to call `SendMessage` to **`team-lead`** (not `main`) with the full report in `message` (not `summary`), AND to write the same report to `docs/phases/<n>/reports/<lane>.md`.
- **Only the QA lane runs the gate.** Concurrent `mise run check` runs in one tree collide on build and coverage outputs. Code-review, simplification, and security lanes are read-only (`git`, `grep`, read).
- **A green HEAD says nothing about a dirty working tree.** Verify `git status --porcelain` is empty before reporting a gate result or claiming a fix landed.
- **The changelog guard is a Claude hook, not a git hook.** Commits on `main` are blocked unless `CHANGELOG.md` is staged. Write the changelog entry with the commit, not after. The version guard also requires `git add` and `git commit` as **separate Bash calls** (no `-a`/`-am`, no `&&` chaining).
- **Bash `cd` persists across tool calls.** Use absolute paths or `git -C <dir>` for worktree and module commands; never rely on the cwd a previous call left behind.
- **Public repo from commit 1.** Run `scripts/redaction-check.sh` before every commit once it exists; until then grep staged content for vault item names, internal IPs and hostnames, real account/business IDs, tokens, and personal names. Fixture IDs are synthetic.
- **(Phase 0) The lead's own commits need the redaction check too.** The work order `docs/phases/0/plan.md` carried the absolute repo path (the operator's home directory) and only the security lane caught it; it took a `git filter-repo --replace-text` pass on the branch before the first push. Write `<repo root>` in work orders, and run `scripts/redaction-check.sh` on every lead commit, not just implementer commits.
- **(Phase 0) Count the inventory yourself before trusting a number in the spec.** "130 requests" was 108 leaves + 22 subfolders counted as requests; reality is 213. A 5-line Python walk of the collection in stage 1 is cheaper than a wrong batch plan in Phase 2.
- **(Phase 0) Lanes catch different things; run all three even for a scaffold.** Code review found a data bug the golden test could not see (byte-stable is not well-formed); security found a `contents: write` inheritance into the reusable CI workflow and the path leak; simplification found the check-script step list written twice. None overlapped.
- **(Phase 0) Coverage gates need an honest shape, not a filter.** Excluding `main.go` by filename hid tested code and let an all-`main.go` module pass vacuously. The fix that held: `cmd/*/main.go` contains exactly one statement, everything else lives in a tested package, and an empty filtered profile is a hard fail.
- **(Phase 0) Pin third-party actions by SHA and check the major version.** The implementer wrote `jdx/mise-action@v2`; the current release was v4. Verify with `gh api repos/<owner>/<repo>/releases/latest` before pinning.
- **(Phase 0) Duplicate agent deliveries happen.** A lane may re-send its report when it sees a second prompt; treat the second delivery as a no-op rather than re-triaging.
- **(Phase 1) Attended consent steps: build everything first, then present one URL per click.** The probe program ran headless in the background with its authorize URL surfaced in chat; two clicks covered both endpoint sets. A background task waiting on a browser callback trips the harness's "appears to be waiting for interactive input" warning -- false alarm, do not kill it.
- **(Phase 1) The permission classifier can block `cp` from the session scratchpad into the repo.** Route the content through Read + Write instead of fighting it.
- **(Phase 1) Commit the read-only lanes' reports BEFORE dispatching QA, and tell QA its own uncommitted report is the one allowed dirty file** -- otherwise its clean-tree check false-fails on files the process itself created.
- **(Phase 1) Independent lanes converge on real blockers.** All three judging lanes found the same `Token.String` method-set leak with no shared context; the one-tier-up reviewer rule and lane independence both paid. Also: a lane can be wrong -- security claimed govulncheck never ran, QA refuted it from `scripts/check.sh`; triage catches lane-vs-lane conflicts.
- **(Phase 1) Give the implementer the triage as a numbered fix list with agreed actions (F1-F11) and the explicit non-applies.** The fix commit came back complete, on-message, and green on the first try; ambiguity in a fix order is where second fix commits come from.
- **(Phase 2) Never pipe the gate through `tail`/`head`.** `mise run check 2>&1 | tail` reports tail's exit code and once masked a red gate. Redirect to a log file and capture `$?` directly.
- **(Phase 2) Fixtures authored from the struct mask decode failures.** Every batch's QA blockers were latent decode failures on fields null or absent in the fixtures. The countermeasure that caught all of them: a QA probe decoding the repo's CAPTURED Postman response bodies through the read models (zero failures required, silent-drop counts reported). Make it a standing QA step; seed fixtures from captures with synthetic IDs, never from the struct.
- **(Phase 2) Parallel worktrees cannot inherit mid-phase conventions.** Batch a's gate created `pathSegment`/`listOpts`/`newPage`/`softDelete`; batches b/c/d, written concurrently, all lacked them and every later gate re-found the same classes. Expected cost of parallelism: prime later lanes with the earlier gates' bug classes (it worked -- they found them all), and budget one convergence pass per batch.
- **(Phase 2) Sequential merge + rebase-before-gate works.** ignore.list/CHANGELOG/spec conflicts were keep-both or drop-both resolutions; only the transport collision (two batches redesigning one file) needed lead judgment, and the next gate reviewed that merge explicitly.
- **(Phase 2) QA findings can be wrong too.** A QA blocker mandated a type change based on a value read through Python's `print()` (which strips quotes); the fix regressed decode and QA's own probe caught it on re-verify. Verify a finding's evidence shape with `repr`, and re-run the acceptance probe after every fix commit.
- **(Phase 2) The dirty-tree guard and the QA lane's uncommitted report fight.** QA must run the gate before writing its report (or stash it); teach `scripts/check.sh` to ignore `docs/phases/*/reports/` eventually (backlog).
- **(Phase 2) A lane may deliver its report via SendMessage but never write the file**, even when told twice. Check the file exists before the reports commit; transcribe from the message when missing.
- **(Phase 2) Agent hygiene:** TaskStop lane agents right after triage, implementer+QA after the merge (now a CLAUDE.md convention). Duplicate dispatch deliveries still happen; agents handled them as no-ops when the work order said to.
- **(Phase 3) Probe the SDK against every lib type in stage 1, not in the implementer's lap.** A 40-line reflection probe found go-sdk's schema inference failing on 54 of 169 lib types before any code was written; the verified three-override recipe went into the work order and the implementer never hit it. Cheapest premise check of the project so far.
- **(Phase 3) Framework code that runs before your handler is inside your trust boundary.** The module's error mapping was careful, but go-sdk validates arguments first and quotes the offending value; the written "never echo card input" constraint was false on that path. Security caught it by reading the SDK, not the module. Write constraint tests with schema-INVALID input as well as the happy path.
- **(Phase 3) A round-trip test that asserts only "no error" is mutation-blind.** 168 rows passed while swapping `bills_delete`/`bills_archive` bodies or crossing ID families would also have passed. Put the exact assertions (recorded method, path with scope id, body for writes, query for options) in the work order's acceptance criteria and have code review check the tests against that sentence.
- **(Phase 3) Capture the tool manifest before and after a refactor commit.** `freshbooks-mcp tools > before.json` / `diff` after the fix commit is a zero-cost proof that fifteen fixes changed no name, description, annotation, or schema. Any phase with a generated surface (Phase 4's `docs/cli.md`) should do the same.
- **(Phase 3) Inspect real wire output, not just decode success.** Four lanes missed that `Page[T]` and `User` have no `json` tags because nothing looked at a marshaled result; QA's own probe of one result per family found 23 tools emitting Go-cased keys. Make "print one actual result per family" a standing QA step alongside the captured-response decode probe.
- **(Phase 3) `mise exec -- go`, never bare `go`, for probes.** The machine's bare `go` was version-skewed against mise's pinned toolchain mid-run (`compile: version mismatch`); only `mise exec` matched what the gate runs.

- **(Phase 4) A 30-item fix order overflowed one sonnet context.** The implementer died at F9 with 33 files of uncommitted work; a second agent recovered it only because the tree was intact. Now standing: fix orders over ~12 items mandate checkpoint commits (`fix(<scope>): ... (Fa-Fb)`, each green), and every implementer prompt carries the context-budget rules (grep/tail, gate logs to a file, never cat a large file).
- **(Phase 4) A resumed agent keeps its context; a respawned one does not.** The QA lane dropped on an API error mid-probe; `SendMessage` to the same name resumed it from its transcript with all its evidence intact. Try resume before respawn.
- **(Phase 4) "Verified the env var is read" is not "verified it has an effect".** QA's docs audit checked that each documented env var appeared in the code; `FRESHBOOKS_LOG_LEVEL` was read and then shadowed by a non-empty flag default. Probe the behaviour (set the var, observe the change), not the grep.
- **(Phase 4) An agent will read "the lead's job" as "not mine" even when the order says otherwise.** The spec callout (F7) was skipped by the fix agent because the triage mentioned the lead in the same sentence. Put lead-owned items in their own section, never inline with implementer items.
- **(Phase 4) Read-only lanes still dirty the tree.** `go list -m all` in a workspace rewrites `go.work.sum`; the security lane noticed and restored it once, the code-review lane flagged it, QA had to check it. Tell read-only lanes to `git checkout -- go.work.sum` after any `go list`, and track `go.work.sum` (now done).
- **(Phase 4) Cross-check generated expectations against the vendor oracle, not the implementation.** The first round-trip `wantPath` table was captured from the code it tested (circular); resolving each command's inventory key against `inventory.json` for method AND path turned "the CLI still does what it did" into "the CLI does what FreshBooks documents" and surfaced one genuine collection artifact (`Projects/Delete Project`).
- **(Phase 4) The lead can land one-line fixes itself.** R1 (a flag default shadowing its env twin) was a one-line change plus two subtests; doing it in the main loop and having QA re-probe cost minutes, where another fix-agent round would have cost an hour.

- **(Phase 5) Dry-run the release mechanics in stage 1, against a real tag in a scratch clone.** The Phase 0 note said "goreleaser OSS has no tag-prefix support, so the workflow strips it" and every later phase inherited that as fact. Reading goreleaser's git pipe plus one `git clone` + `git tag mcp/v0.1.0` + `goreleaser release --skip=publish` showed three breaks (validation fails, the release would land on a new `v0.1.0` tag, both modules' archives share one name) before the implementer was dispatched. A scaffolded release path that has never run against a tag is INFERRED, not CONFIRMED.
- **(Phase 5) Acceptance criteria that name a field are stronger than "parses".** The work order said `jq .bomFormat`; the implementer substituted a generic JSON parse and the SPDX-vs-CycloneDX mismatch survived into five files until code review read `dist/config.yaml`. Keep field-level assertions in the order and have code review check the report against them.
- **(Phase 5) A fix list of a dozen text edits is cheaper in the lead's hands than in another agent round.** Eleven findings (one blocking) landed as one lead commit in the time it takes to brief an agent; the gate re-ran once. Reserve the fix-agent round for findings that need code judgment.
- **(Phase 5) `mise exec` outside the repo uses the global pin.** QA's `go install` probe from `/tmp` hit `compile: version mismatch` because the repo's `mise.toml` is not in scope there; the pinned toolchain's absolute path under `~/.local/share/mise/installs/go/<ver>/bin/go` is the reliable form for out-of-tree probes.
- **(Phase 5) QA passed first round when the earlier lanes' findings were already landed and the probes were spelled out with expected values.** Four mandatory probes with exact assertions (archive names, `version` strings, checksum command, SBOM field) left nothing to interpret; the three advisories were all doc consistency.

## Reference

- **Spec:** `docs/superpowers/specs/2026-08-22-freshbooks-tools-design.md`. Section 2 is locked; sections 3-8 are the build oracle; section 9 is this process.
- **Conventions + dispatch rules:** `CLAUDE.md`. **Status:** `docs/progress.md`.
- **Work orders:** `docs/phases/_templates/{implementer,code-review,simplify,security,qa}.md`. Phase artifacts: `docs/phases/<n>/reports/`. The definitive lib-method table (168 rows, inventory keys, annotation classes): `docs/phases/3/tools.md`.
- **Inventory:** `freshbooks/internal/inventory/testdata/inventory.json` (generated from `freshbooks.postman_collection.json` beside it; `ignore.list` holds the todo/ignore directives). 213 requests in 14 folders / 22 subfolders: Authorization 4, Clients 13, Invoices 50, Expenses 29, Estimates 8, Time Tracking 7, Projects 19, My Team 7, Reports 15, Accounting 19, Uploader 3, Webhooks 5, Settings 28, Tokenization 6. Keys are `<Folder>/<Subfolder>/<Request name>`; name collisions carry a ` (METHOD)` suffix on every side.
- **FreshBooks docs:** https://www.freshbooks.com/api/start (overview), `/api/authentication`, `/api/scopes`, and one page per resource (`/api/invoices`, `/api/clients`, `/api/expenses`, `/api/projects`, `/api/timetracking`, ...) -- these carry the response examples the Postman collection lacks. Every implementer reads the pages for its phase before coding.
- **Toolchain:** everything through `mise run <task>` / `mise exec --`. `mise run check` is the gate.
- **Exemplars to mirror:** within this repo, the most recently shipped phase (`mcp/internal/tools/registry.go` for a data-driven command/tool registry). Outside: the official FreshBooks Python/Node SDKs (resource naming), `google/go-github` (service-struct client shape), `kubectl`/`gh` (CLI ergonomics), the go-sdk `examples/` directory (stateless streamable HTTP).

## Retarget / roadmap

One row per phase; strike and mark `SHIPPED` as they land. Effort is relative; "attended" means the lead should have Wes available.

| Phase | Branch | Implementer -> Reviewers | Effort | Notes |
|---|---|---|---|---|
| ~~0 Scaffold~~ **SHIPPED** | `phase-0/scaffold` -> `main` @ `b3063ba` | sonnet -> opus | low-med | Lesson: the spec's inventory count was wrong (213, not 130) and the scaffold's first coverage gate was dishonest; both caught in stage 1 / the gate, not after. |
| ~~1 Lib core~~ **SHIPPED** | `phase-1/lib-core` -> `main` @ `98ea08c` | opus -> fable | med-high | Lesson: all three judging lanes independently converged on one real blocker (a Go method-set subtlety in the secret-redaction path); the live OAuth probe found expiry semantics (`created_at + expires_in`) no doc states. |
| ~~2 Lib resources~~ **SHIPPED** | `phase-2/{a,b,c,d}` -> `main`, 4 sequential merges, 2026-09-01 | sonnet x4 -> opus | med (volume) | 213/213 keys, todo 0. Lesson: every batch's real blockers were latent decode failures invisible to struct-derived fixtures; the captured-response decode probe (invented mid-phase by QA) caught all of them and is now a standing QA step. Parallel batches re-introduced batch a's fixed bug classes; priming later lanes with earlier gates' findings converged them |
| ~~3 MCP server~~ **SHIPPED** | `phase-3/mcp` -> `main` @ `7ed5891`, 2026-09-01 | sonnet -> opus | med | 168 tools / 212 keys. Lesson: stage 1's SDK reflection probe removed the phase's biggest unknown before dispatch; the two real blockers were both "the test or the framework, not the code" -- a mutation-blind round-trip test and the SDK's pre-handler validator echoing card input -- and neither lane's finding overlapped the other's |
| ~~4 CLI~~ **SHIPPED** | `phase-4/cli` -> `main` @ `3ae853d`, 2026-09-02 | sonnet -> opus | med | 168 commands + `--all`. Lesson: two lanes converged on a missing-`state` CSRF hole and an unvalidated context name; the 30-item fix order overflowed one context and a second agent recovered the uncommitted tree; QA needed three rounds because each fix exposed the next contract gap (exit codes, then an inert env twin). Vendor-inventory cross-check of every command's method+path is now standing |
| ~~5 Release hardening~~ **SHIPPED** | `phase-5/release` -> `main` @ `6cbe6e4`, 2026-09-02 | sonnet -> opus | low-med | Lesson: stage 1's scratch-clone dry run found the release workflow could not ship a prefixed tag at all (goreleaser validates and releases on the stripped tag; archives collided; syft absent) -- the phase's real work was known before dispatch. One blocking review finding (SBOM format claim), lead-landed fix, QA PASS first round |
| **6 v0.1.0 tags** | none (on `main`) | lead only, **attended** | low | Runbook in `docs/progress.md`: lib tag -> `go.mod` bump -> mcp tag -> cli tag, stopping for Wes at each push; repository tag ruleset pre-flight |
| **7 Live conformance** | `phase-7/live` | opus -> fable, **attended** | med | `live` suite against a real account via `fnox exec`; every docs-only `STATE AS OF` fact upgraded or corrected with a capture; MCP + CLI end to end with a real token

**Row 7 is the former "alternative phase"**: it can still run any time, but the default order is tags first so the live pass exercises the released binaries.
