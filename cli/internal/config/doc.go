// Package config will hold the freshbooks CLI's configuration file
// handling ($XDG_CONFIG_HOME/freshbooks/config.yaml), contexts, and the
// flag > env > config precedence rules. Phase 4 fills this in; see
// docs/cli.md.
package config
