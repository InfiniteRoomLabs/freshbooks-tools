# freshbooks CLI reference

A kubectl-style command-line client for the FreshBooks REST API. This file's "Command reference" section below is generated by `mise run docs` (`scripts/docs.sh`, which runs `freshbooks docs`) from the cobra command tree; do not hand-edit past that point.

## Install

Download a release archive from the `cli/vX.Y.Z` GitHub releases, or build from source: `go build ./cli/cmd/freshbooks` from the repository root (requires the `go.work` workspace, so build from inside the repo).

## First login

1. Register an app at https://www.freshbooks.com/api/start and note its client id and secret. Its registered redirect URI must be `https://localhost:8765/callback` (or whatever `--callback-port` you pass).
2. Set `FRESHBOOKS_CLIENT_ID` and `FRESHBOOKS_CLIENT_SECRET` and run `freshbooks auth login` (or pass `--client-id <id> --client-secret <secret>` directly, though that puts the secret in `ps` output and shell history -- see Security notes below). This opens a browser to FreshBooks' consent screen and starts a loopback HTTPS listener on 127.0.0.1 to receive the redirect.
3. Your browser will warn that the certificate is untrusted -- this is expected. The CLI generates a throwaway, self-signed certificate for this one callback, in-process, and never writes it to disk. Accept the warning to continue.
4. On success the CLI stores the token under `$XDG_CONFIG_HOME/freshbooks/credentials/<context>.json` (falling back to `~/.config/freshbooks/credentials/<context>.json` if `$XDG_CONFIG_HOME` is unset -- the common case on macOS and most Linux desktops), mode 0600, and prints nothing from the token itself.
5. Over SSH or in an environment with no browser, pass `--no-browser`: the CLI prints the authorization URL and reads either the full redirected URL or a bare authorization code from stdin instead of listening.

## Contexts

A context is a named account/business/business-uuid triple plus its own credentials file. `freshbooks config set-context <name> --account ... --business ...` creates or updates one; `freshbooks config use-context <name>` switches `config.yaml`'s current context; `freshbooks config contexts` lists them; `freshbooks config view` prints the resolved file. `config.yaml` never carries a secret -- credentials are a separate file per context, managed by `freshbooks auth`.

The scope flags (`--account`/`--business`/`--business-uuid`) and `--context` resolve in this order: an explicit flag, then an environment variable, then the current context's config.yaml value, then (for `--context`) the built-in default `"default"`. An empty environment variable counts as unset, not as an explicit empty value. Every other global flag resolves flag then env then its own built-in default -- config.yaml is consulted only for the scope fields and the current context, never for `--output`, `--base-url`, `--timeout`, or `--log-level`. The remaining global flags are booleans (`--dry-run`, `--yes`, `--no-headers`, `-q/--quiet`) and have no environment twin at all; nobody has asked for one (`FRESHBOOKS_YES`, say), and adding one before someone needs it would just be more surface to keep documented.

The nine environment variables the resolution chain above actually reads:

| Variable | Resolves | Falls back to |
|---|---|---|
| `FRESHBOOKS_CONTEXT` | `--context` | config.yaml `current-context`, then `"default"` |
| `FRESHBOOKS_ACCOUNT_ID` | `--account` | the current context's config.yaml account |
| `FRESHBOOKS_BUSINESS_ID` | `--business` | the current context's config.yaml business |
| `FRESHBOOKS_BUSINESS_UUID` | `--business-uuid` | the current context's config.yaml business-uuid |
| `FRESHBOOKS_CONFIG` | `--config` | `$XDG_CONFIG_HOME/freshbooks/config.yaml`, or `~/.config/freshbooks/config.yaml` if `$XDG_CONFIG_HOME` is unset |
| `FRESHBOOKS_OUTPUT` | `-o/--output` | `table` on a TTY, `json` otherwise (does not apply to the two Binary commands, which read their local `-o` file-path flag directly and ignore this one entirely) |
| `FRESHBOOKS_BASE_URL` | `--base-url` | the lib's default API base URL (`--base-url` itself is hidden from `--help` -- it exists for testing and sandboxes, not everyday use) |
| `FRESHBOOKS_TIMEOUT` | `--timeout` | `30s` |
| `FRESHBOOKS_LOG_LEVEL` | `--log-level` | `warn` |

`FRESHBOOKS_CLIENT_ID` and `FRESHBOOKS_CLIENT_SECRET` are read too, but outside this chain: registry commands have no `--client-id`/`--client-secret` flags of their own (only `auth login`/`auth token`/`auth logout` take those directly), so a registry command's token-refresh path falls back to these two env vars with nothing above them to override.

## Output formats

`-o/--output` selects `json`, `yaml`, `table`, or `name`. The default is `table` when stdout is a terminal and `json` otherwise, so scripts and pipelines get structured output without passing the flag explicitly. `--no-headers` suppresses the header row in table output; `-q/--quiet` suppresses non-result chatter (never errors).

## Exit codes

| Code | Meaning |
|---|---|
| 0 | success |
| 1 | an API or other runtime error |
| 2 | a usage error, for example: a bad flag, an unrecognized --log-level, a malformed --file body, a missing scope, --all combined with --page/--per-page, --dry-run on an auth or config command, a destructive command run without --yes on a TTY, -o/--output on a binary command refusing to overwrite an existing file (without --force) or write to a TTY, a wrong positional argument count, a non-integer --account/--business, an invalid FRESHBOOKS_TIMEOUT, a --query parse failure, a --file open/read failure, an unknown context passed to config use-context, or missing client credentials on auth login |
| 3 | an auth error: no stored credentials for the context, or the API answered 401 |
| 4 | the API answered 404 |

Under `-o json`, an error is one JSON object on stderr: `{"error": {"status", "code", "message", "field", "family", "exit"}}`. Otherwise it is one human-readable line on stderr.

## Automation notes

Every registry command accepts `--dry-run`, which prints the request's method and URL (and its body, for a write) to stdout and sends nothing -- useful for verifying a command's shape before running it for real. `auth` and `config` commands reject `--dry-run` outright (exit 2) instead of silently ignoring it: there is no request to preview, and `auth logout` in particular really does revoke and delete on every run.

Destructive commands -- the ones whose `Short` help line in the command reference below ends with " (destructive: requires --yes on a TTY)" -- refuse to run without `--yes` when stdin is a terminal.

A Binary command's `-o <file>` (`invoices pdf`, `reports download-invoice-details-csv`) refuses to overwrite an existing file without `--force`, and refuses `-o -` when stdout is a terminal (binary bytes would corrupt it). This `-o` is a different flag from the global `-o/--output` formatting flag above and shadows it locally on these two commands; similarly, `auth login --login-timeout` (how long to wait for the browser callback) is a different flag from the global `--timeout` (per-request timeout) -- named differently precisely so it does not shadow it. The scopes `auth login` requests (the full documented set by default, or an explicit `--scopes` list) must all be enabled on the app in the FreshBooks developer portal; otherwise the consent page answers "The requested scope is invalid, unknown, or malformed" and nothing is stored.

## The api escape hatch

`freshbooks api <METHOD> <path> [-f file|-] [--query key=value]...` calls the same authenticated, retrying transport every registry command uses, for an endpoint this CLI does not model as its own command yet. path is rooted at the API base, e.g. `/accounting/account/ACM123/systems/systems/1`.

## Security notes

- The access token is printed by exactly one command: `freshbooks auth token`. Every other command, including `--dry-run`, `config view`, and `auth status`, never prints it.
- Credentials files are written 0600 inside a 0700 directory.
- The loopback login listener's TLS certificate is generated fresh for each login, in-process, and never touches disk.
- `identity applications` redacts every `client_secret` in its output by default; pass `--show-secrets` to include them. `identity create-application` and `identity update-application` always print the `client_secret` in their result -- that is the only time it is shown, since it cannot be retrieved later. Passing `--client-secret` (or `--client-id`) on the command line to `auth login`/`auth token`/`auth logout` puts it in `ps` output and shell history; prefer `FRESHBOOKS_CLIENT_ID`/`FRESHBOOKS_CLIENT_SECRET` instead.

## Command reference

## freshbooks

A kubectl-style CLI for the FreshBooks REST API

### Synopsis

freshbooks is a command-line client for the FreshBooks REST API, backed by the github.com/InfiniteRoomLabs/freshbooks-tools/freshbooks library. See docs/cli.md in the repository for the full command reference.

### Options

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
  -h, --help                   help for freshbooks
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks api](#freshbooks-api)	 - Call an arbitrary FreshBooks API path through the transport's escape hatch
* [freshbooks attachments](#freshbooks-attachments)	 - Manage attachments
* [freshbooks auth](#freshbooks-auth)	 - Log in, check status, log out, or print the access token
* [freshbooks bill-payments](#freshbooks-bill-payments)	 - Manage bill-payments
* [freshbooks bill-vendors](#freshbooks-bill-vendors)	 - Manage bill-vendors
* [freshbooks bills](#freshbooks-bills)	 - Manage bills
* [freshbooks callbacks](#freshbooks-callbacks)	 - Manage callbacks
* [freshbooks clients](#freshbooks-clients)	 - Manage clients
* [freshbooks completion](#freshbooks-completion)	 - Generate the autocompletion script for the specified shell
* [freshbooks config](#freshbooks-config)	 - View and manage config.yaml contexts
* [freshbooks contacts](#freshbooks-contacts)	 - Manage contacts
* [freshbooks credit-notes](#freshbooks-credit-notes)	 - Manage credit-notes
* [freshbooks estimates](#freshbooks-estimates)	 - Manage estimates
* [freshbooks expense-categories](#freshbooks-expense-categories)	 - Manage expense-categories
* [freshbooks expenses](#freshbooks-expenses)	 - Manage expenses
* [freshbooks gateways](#freshbooks-gateways)	 - Manage gateways
* [freshbooks identity](#freshbooks-identity)	 - Manage identity
* [freshbooks images](#freshbooks-images)	 - Manage images
* [freshbooks invoice-profiles](#freshbooks-invoice-profiles)	 - Manage invoice-profiles
* [freshbooks invoices](#freshbooks-invoices)	 - Manage invoices
* [freshbooks items](#freshbooks-items)	 - Manage items
* [freshbooks journal-entries](#freshbooks-journal-entries)	 - Manage journal-entries
* [freshbooks journal-entry-accounts](#freshbooks-journal-entry-accounts)	 - Manage journal-entry-accounts
* [freshbooks ledger-accounts](#freshbooks-ledger-accounts)	 - Manage ledger-accounts
* [freshbooks other-income](#freshbooks-other-income)	 - Manage other-income
* [freshbooks payment-options](#freshbooks-payment-options)	 - Manage payment-options
* [freshbooks payments](#freshbooks-payments)	 - Manage payments
* [freshbooks projects](#freshbooks-projects)	 - Manage projects
* [freshbooks reports](#freshbooks-reports)	 - Manage reports
* [freshbooks retainers](#freshbooks-retainers)	 - Manage retainers
* [freshbooks service-rates](#freshbooks-service-rates)	 - Manage service-rates
* [freshbooks services](#freshbooks-services)	 - Manage services
* [freshbooks staff](#freshbooks-staff)	 - Manage staff
* [freshbooks systems](#freshbooks-systems)	 - Manage systems
* [freshbooks tasks](#freshbooks-tasks)	 - Manage tasks
* [freshbooks taxes](#freshbooks-taxes)	 - Manage taxes
* [freshbooks team-members](#freshbooks-team-members)	 - Manage team-members
* [freshbooks time-entries](#freshbooks-time-entries)	 - Manage time-entries
* [freshbooks version](#freshbooks-version)	 - Print the freshbooks CLI version

## freshbooks api

Call an arbitrary FreshBooks API path through the transport's escape hatch

### Synopsis

api sends one request through the same authenticated, retrying transport every registry command uses, for an endpoint this CLI does not model as its own command yet. path is rooted at the API base, e.g. /accounting/account/ACM123/systems/systems/1.

```
freshbooks api <METHOD> <path> [flags]
```

### Options

```
  -f, --file string         JSON request body: a file path, or - for stdin
  -h, --help                help for api
      --query stringArray   query parameter as key=value (repeatable)
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks](#freshbooks)	 - A kubectl-style CLI for the FreshBooks REST API

## freshbooks attachments

Manage attachments

### Options

```
  -h, --help   help for attachments
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks](#freshbooks)	 - A kubectl-style CLI for the FreshBooks REST API
* [freshbooks attachments upload-expense-receipt](#freshbooks-attachments-upload-expense-receipt)	 - Upload a receipt image for an expense

## freshbooks attachments upload-expense-receipt

Upload a receipt image for an expense

```
freshbooks attachments upload-expense-receipt [flags]
```

### Options

```
      --file string   path to the local file to upload (required)
  -h, --help          help for upload-expense-receipt
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks attachments](#freshbooks-attachments)	 - Manage attachments

## freshbooks auth

Log in, check status, log out, or print the access token

### Options

```
  -h, --help   help for auth
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks](#freshbooks)	 - A kubectl-style CLI for the FreshBooks REST API
* [freshbooks auth login](#freshbooks-auth-login)	 - Authorize the CLI against FreshBooks
* [freshbooks auth logout](#freshbooks-auth-logout)	 - Revoke and remove the current context's credentials
* [freshbooks auth status](#freshbooks-auth-status)	 - Show the current context's credential state (never the token)
* [freshbooks auth token](#freshbooks-auth-token)	 - Print the current context's access token (the one place this CLI ever prints one)

## freshbooks auth login

Authorize the CLI against FreshBooks

```
freshbooks auth login [flags]
```

### Options

```
      --callback-port int        loopback port for the browser callback (default 8765)
      --client-id string         the registered application's client id (default: FRESHBOOKS_CLIENT_ID)
      --client-secret string     the registered application's client secret (default: FRESHBOOKS_CLIENT_SECRET)
  -h, --help                     help for login
      --login-timeout duration   how long to wait for the browser callback (default 5m0s)
      --no-browser               print the URL and read the redirect (or a bare code) from stdin instead of opening a browser
      --scopes stringArray       OAuth scopes to request (default: the full documented user:*:read/write set)
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks auth](#freshbooks-auth)	 - Log in, check status, log out, or print the access token

## freshbooks auth logout

Revoke and remove the current context's credentials

```
freshbooks auth logout [flags]
```

### Options

```
      --client-id string       the registered application's client id (default: FRESHBOOKS_CLIENT_ID)
      --client-secret string   the registered application's client secret (default: FRESHBOOKS_CLIENT_SECRET)
  -h, --help                   help for logout
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks auth](#freshbooks-auth)	 - Log in, check status, log out, or print the access token

## freshbooks auth status

Show the current context's credential state (never the token)

```
freshbooks auth status [flags]
```

### Options

```
  -h, --help   help for status
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks auth](#freshbooks-auth)	 - Log in, check status, log out, or print the access token

## freshbooks auth token

Print the current context's access token (the one place this CLI ever prints one)

```
freshbooks auth token [flags]
```

### Options

```
      --client-id string       the registered application's client id (default: FRESHBOOKS_CLIENT_ID)
      --client-secret string   the registered application's client secret (default: FRESHBOOKS_CLIENT_SECRET)
  -h, --help                   help for token
      --refresh                force a refresh, rotating and persisting the token pair before printing
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks auth](#freshbooks-auth)	 - Log in, check status, log out, or print the access token

## freshbooks bill-payments

Manage bill-payments

### Options

```
  -h, --help   help for bill-payments
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks](#freshbooks)	 - A kubectl-style CLI for the FreshBooks REST API
* [freshbooks bill-payments create](#freshbooks-bill-payments-create)	 - Record a payment against a bill
* [freshbooks bill-payments update](#freshbooks-bill-payments-update)	 - Update a payment against a bill

## freshbooks bill-payments create

Record a payment against a bill

```
freshbooks bill-payments create [flags]
```

### Options

```
  -f, --file string   JSON request body: a file path, or - for stdin (required)
  -h, --help          help for create
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks bill-payments](#freshbooks-bill-payments)	 - Manage bill-payments

## freshbooks bill-payments update

Update a payment against a bill

```
freshbooks bill-payments update <id> [flags]
```

### Options

```
  -f, --file string   JSON request body: a file path, or - for stdin (required)
  -h, --help          help for update
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks bill-payments](#freshbooks-bill-payments)	 - Manage bill-payments

## freshbooks bill-vendors

Manage bill-vendors

### Options

```
  -h, --help   help for bill-vendors
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks](#freshbooks)	 - A kubectl-style CLI for the FreshBooks REST API
* [freshbooks bill-vendors create](#freshbooks-bill-vendors-create)	 - Add a bill vendor
* [freshbooks bill-vendors delete](#freshbooks-bill-vendors-delete)	 - Delete a bill vendor (destructive: requires --yes on a TTY)
* [freshbooks bill-vendors list](#freshbooks-bill-vendors-list)	 - List a business's bill vendors
* [freshbooks bill-vendors update](#freshbooks-bill-vendors-update)	 - Update a bill vendor's details

## freshbooks bill-vendors create

Add a bill vendor

```
freshbooks bill-vendors create [flags]
```

### Options

```
  -f, --file string   JSON request body: a file path, or - for stdin (required)
  -h, --help          help for create
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks bill-vendors](#freshbooks-bill-vendors)	 - Manage bill-vendors

## freshbooks bill-vendors delete

Delete a bill vendor (destructive: requires --yes on a TTY)

```
freshbooks bill-vendors delete <id> [flags]
```

### Options

```
  -h, --help   help for delete
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks bill-vendors](#freshbooks-bill-vendors)	 - Manage bill-vendors

## freshbooks bill-vendors list

List a business's bill vendors

```
freshbooks bill-vendors list [flags]
```

### Options

```
      --all                     walk every page and return every item (rejects --page/--per-page)
  -h, --help                    help for list
      --page int                1-based page number
      --per-page int            page size
      --search stringToString   filter as key=value (repeatable) (default [])
      --sort string             sort as field[:asc|desc] (default asc; direction encoding for business-scoped resources is unconfirmed against the live API -- see docs/progress.md)
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks bill-vendors](#freshbooks-bill-vendors)	 - Manage bill-vendors

## freshbooks bill-vendors update

Update a bill vendor's details

```
freshbooks bill-vendors update <id> [flags]
```

### Options

```
  -f, --file string   JSON request body: a file path, or - for stdin (required)
  -h, --help          help for update
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks bill-vendors](#freshbooks-bill-vendors)	 - Manage bill-vendors

## freshbooks bills

Manage bills

### Options

```
  -h, --help   help for bills
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks](#freshbooks)	 - A kubectl-style CLI for the FreshBooks REST API
* [freshbooks bills archive](#freshbooks-bills-archive)	 - Archive a vendor bill (destructive: requires --yes on a TTY)
* [freshbooks bills create](#freshbooks-bills-create)	 - Add a bill from a vendor
* [freshbooks bills delete](#freshbooks-bills-delete)	 - Delete a vendor bill (destructive: requires --yes on a TTY)
* [freshbooks bills list](#freshbooks-bills-list)	 - List a business's vendor bills

## freshbooks bills archive

Archive a vendor bill (destructive: requires --yes on a TTY)

```
freshbooks bills archive <id> [flags]
```

### Options

```
  -h, --help   help for archive
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks bills](#freshbooks-bills)	 - Manage bills

## freshbooks bills create

Add a bill from a vendor

```
freshbooks bills create [flags]
```

### Options

```
  -f, --file string   JSON request body: a file path, or - for stdin (required)
  -h, --help          help for create
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks bills](#freshbooks-bills)	 - Manage bills

## freshbooks bills delete

Delete a vendor bill (destructive: requires --yes on a TTY)

```
freshbooks bills delete <id> [flags]
```

### Options

```
  -h, --help   help for delete
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks bills](#freshbooks-bills)	 - Manage bills

## freshbooks bills list

List a business's vendor bills

```
freshbooks bills list [flags]
```

### Options

```
      --all                     walk every page and return every item (rejects --page/--per-page)
  -h, --help                    help for list
      --page int                1-based page number
      --per-page int            page size
      --search stringToString   filter as key=value (repeatable) (default [])
      --sort string             sort as field[:asc|desc] (default asc; direction encoding for business-scoped resources is unconfirmed against the live API -- see docs/progress.md)
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks bills](#freshbooks-bills)	 - Manage bills

## freshbooks callbacks

Manage callbacks

### Options

```
  -h, --help   help for callbacks
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks](#freshbooks)	 - A kubectl-style CLI for the FreshBooks REST API
* [freshbooks callbacks delete](#freshbooks-callbacks-delete)	 - Delete a webhook callback (destructive: requires --yes on a TTY)
* [freshbooks callbacks list](#freshbooks-callbacks-list)	 - List webhook callbacks
* [freshbooks callbacks register](#freshbooks-callbacks-register)	 - Register a webhook callback
* [freshbooks callbacks resend-verification](#freshbooks-callbacks-resend-verification)	 - Resend a webhook callback's verification code
* [freshbooks callbacks verify](#freshbooks-callbacks-verify)	 - Verify a webhook callback

## freshbooks callbacks delete

Delete a webhook callback (destructive: requires --yes on a TTY)

```
freshbooks callbacks delete <id> [flags]
```

### Options

```
  -h, --help   help for delete
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks callbacks](#freshbooks-callbacks)	 - Manage callbacks

## freshbooks callbacks list

List webhook callbacks

```
freshbooks callbacks list [flags]
```

### Options

```
      --all                     walk every page and return every item (rejects --page/--per-page)
  -h, --help                    help for list
      --page int                1-based page number
      --per-page int            page size
      --search stringToString   filter as key=value (repeatable) (default [])
      --sort string             sort as field[:asc|desc] (default asc; direction encoding for business-scoped resources is unconfirmed against the live API -- see docs/progress.md)
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks callbacks](#freshbooks-callbacks)	 - Manage callbacks

## freshbooks callbacks register

Register a webhook callback

```
freshbooks callbacks register [flags]
```

### Options

```
  -f, --file string   JSON request body: a file path, or - for stdin (required)
  -h, --help          help for register
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks callbacks](#freshbooks-callbacks)	 - Manage callbacks

## freshbooks callbacks resend-verification

Resend a webhook callback's verification code

```
freshbooks callbacks resend-verification <id> [flags]
```

### Options

```
  -h, --help   help for resend-verification
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks callbacks](#freshbooks-callbacks)	 - Manage callbacks

## freshbooks callbacks verify

Verify a webhook callback

```
freshbooks callbacks verify <id> [flags]
```

### Options

```
  -h, --help              help for verify
      --verifier string   the verifier code FreshBooks sent (required)
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks callbacks](#freshbooks-callbacks)	 - Manage callbacks

## freshbooks clients

Manage clients

### Options

```
  -h, --help   help for clients
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks](#freshbooks)	 - A kubectl-style CLI for the FreshBooks REST API
* [freshbooks clients create](#freshbooks-clients-create)	 - Create a new client
* [freshbooks clients get](#freshbooks-clients-get)	 - Get a single client
* [freshbooks clients list](#freshbooks-clients-list)	 - List an account's clients
* [freshbooks clients remove-all-secondary-contacts](#freshbooks-clients-remove-all-secondary-contacts)	 - Remove every secondary contact from a client (destructive: requires --yes on a TTY)
* [freshbooks clients update](#freshbooks-clients-update)	 - Update a client

## freshbooks clients create

Create a new client

```
freshbooks clients create [flags]
```

### Options

```
  -f, --file string   JSON request body: a file path, or - for stdin (required)
  -h, --help          help for create
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks clients](#freshbooks-clients)	 - Manage clients

## freshbooks clients get

Get a single client

```
freshbooks clients get <id> [flags]
```

### Options

```
  -h, --help                  help for get
      --include stringArray   related sub-resource to embed in the response (repeatable)
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks clients](#freshbooks-clients)	 - Manage clients

## freshbooks clients list

List an account's clients

```
freshbooks clients list [flags]
```

### Options

```
      --all                     walk every page and return every item (rejects --page/--per-page)
  -h, --help                    help for list
      --include stringArray     related sub-resource to embed in the response (repeatable)
      --page int                1-based page number
      --per-page int            page size
      --search stringToString   filter as key=value (repeatable) (default [])
      --sort string             sort as field[:asc|desc] (default asc; direction encoding for business-scoped resources is unconfirmed against the live API -- see docs/progress.md)
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks clients](#freshbooks-clients)	 - Manage clients

## freshbooks clients remove-all-secondary-contacts

Remove every secondary contact from a client (destructive: requires --yes on a TTY)

```
freshbooks clients remove-all-secondary-contacts <id> [flags]
```

### Options

```
  -h, --help   help for remove-all-secondary-contacts
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks clients](#freshbooks-clients)	 - Manage clients

## freshbooks clients update

Update a client

```
freshbooks clients update <id> [flags]
```

### Options

```
  -f, --file string   JSON request body: a file path, or - for stdin (required)
  -h, --help          help for update
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks clients](#freshbooks-clients)	 - Manage clients

## freshbooks completion

Generate the autocompletion script for the specified shell

### Synopsis

Generate the autocompletion script for freshbooks for the specified shell.
See each sub-command's help for details on how to use the generated script.


### Options

```
  -h, --help   help for completion
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks](#freshbooks)	 - A kubectl-style CLI for the FreshBooks REST API
* [freshbooks completion bash](#freshbooks-completion-bash)	 - Generate the autocompletion script for bash
* [freshbooks completion fish](#freshbooks-completion-fish)	 - Generate the autocompletion script for fish
* [freshbooks completion powershell](#freshbooks-completion-powershell)	 - Generate the autocompletion script for powershell
* [freshbooks completion zsh](#freshbooks-completion-zsh)	 - Generate the autocompletion script for zsh

## freshbooks completion bash

Generate the autocompletion script for bash

### Synopsis

Generate the autocompletion script for the bash shell.

This script depends on the 'bash-completion' package.
If it is not installed already, you can install it via your OS's package manager.

To load completions in your current shell session:

	source <(freshbooks completion bash)

To load completions for every new session, execute once:

#### Linux:

	freshbooks completion bash > /etc/bash_completion.d/freshbooks

#### macOS:

	freshbooks completion bash > $(brew --prefix)/etc/bash_completion.d/freshbooks

You will need to start a new shell for this setup to take effect.


```
freshbooks completion bash
```

### Options

```
  -h, --help              help for bash
      --no-descriptions   disable completion descriptions
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks completion](#freshbooks-completion)	 - Generate the autocompletion script for the specified shell

## freshbooks completion fish

Generate the autocompletion script for fish

### Synopsis

Generate the autocompletion script for the fish shell.

To load completions in your current shell session:

	freshbooks completion fish | source

To load completions for every new session, execute once:

	freshbooks completion fish > ~/.config/fish/completions/freshbooks.fish

You will need to start a new shell for this setup to take effect.


```
freshbooks completion fish [flags]
```

### Options

```
  -h, --help              help for fish
      --no-descriptions   disable completion descriptions
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks completion](#freshbooks-completion)	 - Generate the autocompletion script for the specified shell

## freshbooks completion powershell

Generate the autocompletion script for powershell

### Synopsis

Generate the autocompletion script for powershell.

To load completions in your current shell session:

	freshbooks completion powershell | Out-String | Invoke-Expression

To load completions for every new session, add the output of the above command
to your powershell profile.


```
freshbooks completion powershell [flags]
```

### Options

```
  -h, --help              help for powershell
      --no-descriptions   disable completion descriptions
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks completion](#freshbooks-completion)	 - Generate the autocompletion script for the specified shell

## freshbooks completion zsh

Generate the autocompletion script for zsh

### Synopsis

Generate the autocompletion script for the zsh shell.

If shell completion is not already enabled in your environment you will need
to enable it.  You can execute the following once:

	echo "autoload -U compinit; compinit" >> ~/.zshrc

To load completions in your current shell session:

	source <(freshbooks completion zsh)

To load completions for every new session, execute once:

#### Linux:

	freshbooks completion zsh > "${fpath[1]}/_freshbooks"

#### macOS:

	freshbooks completion zsh > $(brew --prefix)/share/zsh/site-functions/_freshbooks

You will need to start a new shell for this setup to take effect.


```
freshbooks completion zsh [flags]
```

### Options

```
  -h, --help              help for zsh
      --no-descriptions   disable completion descriptions
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks completion](#freshbooks-completion)	 - Generate the autocompletion script for the specified shell

## freshbooks config

View and manage config.yaml contexts

### Options

```
  -h, --help   help for config
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks](#freshbooks)	 - A kubectl-style CLI for the FreshBooks REST API
* [freshbooks config contexts](#freshbooks-config-contexts)	 - List the contexts defined in config.yaml
* [freshbooks config set-context](#freshbooks-config-set-context)	 - Create or update a context's account/business/business-uuid
* [freshbooks config use-context](#freshbooks-config-use-context)	 - Switch config.yaml's current-context
* [freshbooks config view](#freshbooks-config-view)	 - Print the resolved config.yaml

## freshbooks config contexts

List the contexts defined in config.yaml

```
freshbooks config contexts [flags]
```

### Options

```
  -h, --help   help for contexts
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks config](#freshbooks-config)	 - View and manage config.yaml contexts

## freshbooks config set-context

Create or update a context's account/business/business-uuid

```
freshbooks config set-context <name> [flags]
```

### Options

```
      --account string         the context's FreshBooks account id
      --business string        the context's FreshBooks business id
      --business-uuid string   the context's FreshBooks business UUID
  -h, --help                   help for set-context
```

### Options inherited from parent commands

```
      --config string      path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string     config context to use (default: config.yaml's current-context, else "default")
      --dry-run            print the request that would be sent and send nothing
      --log-level string   log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers         suppress the header row in table output
  -o, --output string      output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet              suppress non-result output (errors still print)
      --timeout duration   per-request timeout (default 30s)
      --yes                confirm a destructive command
```

### SEE ALSO

* [freshbooks config](#freshbooks-config)	 - View and manage config.yaml contexts

## freshbooks config use-context

Switch config.yaml's current-context

```
freshbooks config use-context <name> [flags]
```

### Options

```
  -h, --help   help for use-context
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks config](#freshbooks-config)	 - View and manage config.yaml contexts

## freshbooks config view

Print the resolved config.yaml

```
freshbooks config view [flags]
```

### Options

```
  -h, --help   help for view
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks config](#freshbooks-config)	 - View and manage config.yaml contexts

## freshbooks contacts

Manage contacts

### Options

```
  -h, --help   help for contacts
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks](#freshbooks)	 - A kubectl-style CLI for the FreshBooks REST API
* [freshbooks contacts delete](#freshbooks-contacts-delete)	 - Delete a client's secondary contact (destructive: requires --yes on a TTY)
* [freshbooks contacts update](#freshbooks-contacts-update)	 - Update a client's secondary contact

## freshbooks contacts delete

Delete a client's secondary contact (destructive: requires --yes on a TTY)

```
freshbooks contacts delete <id> [flags]
```

### Options

```
  -h, --help   help for delete
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks contacts](#freshbooks-contacts)	 - Manage contacts

## freshbooks contacts update

Update a client's secondary contact

```
freshbooks contacts update <id> [flags]
```

### Options

```
  -f, --file string   JSON request body: a file path, or - for stdin (required)
  -h, --help          help for update
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks contacts](#freshbooks-contacts)	 - Manage contacts

## freshbooks credit-notes

Manage credit-notes

### Options

```
  -h, --help   help for credit-notes
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks](#freshbooks)	 - A kubectl-style CLI for the FreshBooks REST API
* [freshbooks credit-notes create](#freshbooks-credit-notes-create)	 - Create a credit note or prepayment credit
* [freshbooks credit-notes delete](#freshbooks-credit-notes-delete)	 - Delete a credit (destructive: requires --yes on a TTY)
* [freshbooks credit-notes list](#freshbooks-credit-notes-list)	 - List a client's credits
* [freshbooks credit-notes update](#freshbooks-credit-notes-update)	 - Update a credit note or prepayment credit

## freshbooks credit-notes create

Create a credit note or prepayment credit

```
freshbooks credit-notes create [flags]
```

### Options

```
  -f, --file string   JSON request body: a file path, or - for stdin (required)
  -h, --help          help for create
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks credit-notes](#freshbooks-credit-notes)	 - Manage credit-notes

## freshbooks credit-notes delete

Delete a credit (destructive: requires --yes on a TTY)

```
freshbooks credit-notes delete <id> [flags]
```

### Options

```
  -h, --help   help for delete
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks credit-notes](#freshbooks-credit-notes)	 - Manage credit-notes

## freshbooks credit-notes list

List a client's credits

```
freshbooks credit-notes list [flags]
```

### Options

```
      --all                     walk every page and return every item (rejects --page/--per-page)
  -h, --help                    help for list
      --page int                1-based page number
      --per-page int            page size
      --search stringToString   filter as key=value (repeatable) (default [])
      --sort string             sort as field[:asc|desc] (default asc; direction encoding for business-scoped resources is unconfirmed against the live API -- see docs/progress.md)
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks credit-notes](#freshbooks-credit-notes)	 - Manage credit-notes

## freshbooks credit-notes update

Update a credit note or prepayment credit

```
freshbooks credit-notes update <id> [flags]
```

### Options

```
  -f, --file string   JSON request body: a file path, or - for stdin (required)
  -h, --help          help for update
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks credit-notes](#freshbooks-credit-notes)	 - Manage credit-notes

## freshbooks estimates

Manage estimates

### Options

```
  -h, --help   help for estimates
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks](#freshbooks)	 - A kubectl-style CLI for the FreshBooks REST API
* [freshbooks estimates accept](#freshbooks-estimates-accept)	 - Accept an estimate
* [freshbooks estimates create](#freshbooks-estimates-create)	 - Create an estimate
* [freshbooks estimates delete](#freshbooks-estimates-delete)	 - Delete an estimate (destructive: requires --yes on a TTY)
* [freshbooks estimates get](#freshbooks-estimates-get)	 - Get a single estimate
* [freshbooks estimates list](#freshbooks-estimates-list)	 - List estimates
* [freshbooks estimates send](#freshbooks-estimates-send)	 - Send an estimate by email
* [freshbooks estimates update](#freshbooks-estimates-update)	 - Update an estimate

## freshbooks estimates accept

Accept an estimate

```
freshbooks estimates accept <id> [flags]
```

### Options

```
  -h, --help   help for accept
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks estimates](#freshbooks-estimates)	 - Manage estimates

## freshbooks estimates create

Create an estimate

```
freshbooks estimates create [flags]
```

### Options

```
  -f, --file string   JSON request body: a file path, or - for stdin (required)
  -h, --help          help for create
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks estimates](#freshbooks-estimates)	 - Manage estimates

## freshbooks estimates delete

Delete an estimate (destructive: requires --yes on a TTY)

```
freshbooks estimates delete <id> [flags]
```

### Options

```
  -h, --help   help for delete
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks estimates](#freshbooks-estimates)	 - Manage estimates

## freshbooks estimates get

Get a single estimate

```
freshbooks estimates get <id> [flags]
```

### Options

```
  -h, --help                  help for get
      --include stringArray   related sub-resource to embed in the response (repeatable)
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks estimates](#freshbooks-estimates)	 - Manage estimates

## freshbooks estimates list

List estimates

```
freshbooks estimates list [flags]
```

### Options

```
      --all                     walk every page and return every item (rejects --page/--per-page)
  -h, --help                    help for list
      --include stringArray     related sub-resource to embed in the response (repeatable)
      --page int                1-based page number
      --per-page int            page size
      --search stringToString   filter as key=value (repeatable) (default [])
      --sort string             sort as field[:asc|desc] (default asc; direction encoding for business-scoped resources is unconfirmed against the live API -- see docs/progress.md)
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks estimates](#freshbooks-estimates)	 - Manage estimates

## freshbooks estimates send

Send an estimate by email

```
freshbooks estimates send <id> [flags]
```

### Options

```
  -f, --file string   JSON request body: a file path, or - for stdin (required)
  -h, --help          help for send
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks estimates](#freshbooks-estimates)	 - Manage estimates

## freshbooks estimates update

Update an estimate

```
freshbooks estimates update <id> [flags]
```

### Options

```
  -f, --file string   JSON request body: a file path, or - for stdin (required)
  -h, --help          help for update
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks estimates](#freshbooks-estimates)	 - Manage estimates

## freshbooks expense-categories

Manage expense-categories

### Options

```
  -h, --help   help for expense-categories
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks](#freshbooks)	 - A kubectl-style CLI for the FreshBooks REST API
* [freshbooks expense-categories create](#freshbooks-expense-categories-create)	 - Create a custom expense category
* [freshbooks expense-categories get](#freshbooks-expense-categories-get)	 - Get a single expense category
* [freshbooks expense-categories list](#freshbooks-expense-categories-list)	 - List expense categories

## freshbooks expense-categories create

Create a custom expense category

```
freshbooks expense-categories create [flags]
```

### Options

```
  -f, --file string   JSON request body: a file path, or - for stdin (required)
  -h, --help          help for create
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks expense-categories](#freshbooks-expense-categories)	 - Manage expense-categories

## freshbooks expense-categories get

Get a single expense category

```
freshbooks expense-categories get <id> [flags]
```

### Options

```
  -h, --help                  help for get
      --include stringArray   related sub-resource to embed in the response (repeatable)
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks expense-categories](#freshbooks-expense-categories)	 - Manage expense-categories

## freshbooks expense-categories list

List expense categories

```
freshbooks expense-categories list [flags]
```

### Options

```
      --all                     walk every page and return every item (rejects --page/--per-page)
  -h, --help                    help for list
      --page int                1-based page number
      --per-page int            page size
      --search stringToString   filter as key=value (repeatable) (default [])
      --sort string             sort as field[:asc|desc] (default asc; direction encoding for business-scoped resources is unconfirmed against the live API -- see docs/progress.md)
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks expense-categories](#freshbooks-expense-categories)	 - Manage expense-categories

## freshbooks expenses

Manage expenses

### Options

```
  -h, --help   help for expenses
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks](#freshbooks)	 - A kubectl-style CLI for the FreshBooks REST API
* [freshbooks expenses create](#freshbooks-expenses-create)	 - Create an expense
* [freshbooks expenses create-recurring](#freshbooks-expenses-create-recurring)	 - Create a recurring expense profile
* [freshbooks expenses delete](#freshbooks-expenses-delete)	 - Delete an expense (destructive: requires --yes on a TTY)
* [freshbooks expenses get](#freshbooks-expenses-get)	 - Get a single expense
* [freshbooks expenses list](#freshbooks-expenses-list)	 - List expenses
* [freshbooks expenses summaries](#freshbooks-expenses-summaries)	 - Get expense summaries
* [freshbooks expenses update](#freshbooks-expenses-update)	 - Update an expense
* [freshbooks expenses vendors](#freshbooks-expenses-vendors)	 - List distinct expense vendor names

## freshbooks expenses create

Create an expense

```
freshbooks expenses create [flags]
```

### Options

```
  -f, --file string   JSON request body: a file path, or - for stdin (required)
  -h, --help          help for create
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks expenses](#freshbooks-expenses)	 - Manage expenses

## freshbooks expenses create-recurring

Create a recurring expense profile

```
freshbooks expenses create-recurring [flags]
```

### Options

```
  -f, --file string   JSON request body: a file path, or - for stdin (required)
  -h, --help          help for create-recurring
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks expenses](#freshbooks-expenses)	 - Manage expenses

## freshbooks expenses delete

Delete an expense (destructive: requires --yes on a TTY)

```
freshbooks expenses delete <id> [flags]
```

### Options

```
  -h, --help   help for delete
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks expenses](#freshbooks-expenses)	 - Manage expenses

## freshbooks expenses get

Get a single expense

```
freshbooks expenses get <id> [flags]
```

### Options

```
  -h, --help                  help for get
      --include stringArray   related sub-resource to embed in the response (repeatable)
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks expenses](#freshbooks-expenses)	 - Manage expenses

## freshbooks expenses list

List expenses

```
freshbooks expenses list [flags]
```

### Options

```
      --all                     walk every page and return every item (rejects --page/--per-page)
  -h, --help                    help for list
      --page int                1-based page number
      --per-page int            page size
      --search stringToString   filter as key=value (repeatable) (default [])
      --sort string             sort as field[:asc|desc] (default asc; direction encoding for business-scoped resources is unconfirmed against the live API -- see docs/progress.md)
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks expenses](#freshbooks-expenses)	 - Manage expenses

## freshbooks expenses summaries

Get expense summaries

```
freshbooks expenses summaries [flags]
```

### Options

```
  -h, --help   help for summaries
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks expenses](#freshbooks-expenses)	 - Manage expenses

## freshbooks expenses update

Update an expense

```
freshbooks expenses update <id> [flags]
```

### Options

```
  -f, --file string   JSON request body: a file path, or - for stdin (required)
  -h, --help          help for update
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks expenses](#freshbooks-expenses)	 - Manage expenses

## freshbooks expenses vendors

List distinct expense vendor names

```
freshbooks expenses vendors [flags]
```

### Options

```
  -h, --help   help for vendors
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks expenses](#freshbooks-expenses)	 - Manage expenses

## freshbooks gateways

Manage gateways

### Options

```
  -h, --help   help for gateways
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks](#freshbooks)	 - A kubectl-style CLI for the FreshBooks REST API
* [freshbooks gateways get](#freshbooks-gateways-get)	 - List an account's configured payment gateway connections

## freshbooks gateways get

List an account's configured payment gateway connections

```
freshbooks gateways get [flags]
```

### Options

```
  -h, --help   help for get
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks gateways](#freshbooks-gateways)	 - Manage gateways

## freshbooks identity

Manage identity

### Options

```
  -h, --help   help for identity
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks](#freshbooks)	 - A kubectl-style CLI for the FreshBooks REST API
* [freshbooks identity add-business](#freshbooks-identity-add-business)	 - Add a business to the current identity
* [freshbooks identity applications](#freshbooks-identity-applications)	 - List developer applications (client_secret redacted unless --show-secrets)
* [freshbooks identity create-application](#freshbooks-identity-create-application)	 - Register a new developer application (prints the generated client_secret -- it is shown only here and on update)
* [freshbooks identity delete-business](#freshbooks-identity-delete-business)	 - Delete a business (destructive: requires --yes on a TTY)
* [freshbooks identity delete-business-subscription](#freshbooks-identity-delete-business-subscription)	 - Delete a business's subscription (destructive: requires --yes on a TTY)
* [freshbooks identity me](#freshbooks-identity-me)	 - List the memberships of the identity behind the current token
* [freshbooks identity provision-payments](#freshbooks-identity-provision-payments)	 - Provision FreshBooks Payments for an account
* [freshbooks identity register](#freshbooks-identity-register)	 - Register a new FreshBooks user
* [freshbooks identity update-application](#freshbooks-identity-update-application)	 - Update a developer application (prints its client_secret -- it is shown only here and on create)
* [freshbooks identity whoami](#freshbooks-identity-whoami)	 - Show the full identity behind the current token

## freshbooks identity add-business

Add a business to the current identity

```
freshbooks identity add-business [flags]
```

### Options

```
  -f, --file string   JSON request body: a file path, or - for stdin (required)
  -h, --help          help for add-business
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks identity](#freshbooks-identity)	 - Manage identity

## freshbooks identity applications

List developer applications (client_secret redacted unless --show-secrets)

```
freshbooks identity applications [flags]
```

### Options

```
  -h, --help           help for applications
      --show-secrets   include each application's client_secret in the output instead of redacting it
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks identity](#freshbooks-identity)	 - Manage identity

## freshbooks identity create-application

Register a new developer application (prints the generated client_secret -- it is shown only here and on update)

```
freshbooks identity create-application [flags]
```

### Options

```
  -f, --file string   JSON request body: a file path, or - for stdin (required)
  -h, --help          help for create-application
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks identity](#freshbooks-identity)	 - Manage identity

## freshbooks identity delete-business

Delete a business (destructive: requires --yes on a TTY)

```
freshbooks identity delete-business [flags]
```

### Options

```
  -h, --help   help for delete-business
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks identity](#freshbooks-identity)	 - Manage identity

## freshbooks identity delete-business-subscription

Delete a business's subscription (destructive: requires --yes on a TTY)

```
freshbooks identity delete-business-subscription [flags]
```

### Options

```
  -h, --help   help for delete-business-subscription
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks identity](#freshbooks-identity)	 - Manage identity

## freshbooks identity me

List the memberships of the identity behind the current token

```
freshbooks identity me [flags]
```

### Options

```
  -h, --help   help for me
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks identity](#freshbooks-identity)	 - Manage identity

## freshbooks identity provision-payments

Provision FreshBooks Payments for an account

```
freshbooks identity provision-payments [flags]
```

### Options

```
  -f, --file string   JSON request body: a file path, or - for stdin (required)
  -h, --help          help for provision-payments
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks identity](#freshbooks-identity)	 - Manage identity

## freshbooks identity register

Register a new FreshBooks user

```
freshbooks identity register [flags]
```

### Options

```
  -f, --file string   JSON request body: a file path, or - for stdin (required)
  -h, --help          help for register
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks identity](#freshbooks-identity)	 - Manage identity

## freshbooks identity update-application

Update a developer application (prints its client_secret -- it is shown only here and on create)

```
freshbooks identity update-application <client-id> [flags]
```

### Options

```
  -f, --file string   JSON request body: a file path, or - for stdin (required)
  -h, --help          help for update-application
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks identity](#freshbooks-identity)	 - Manage identity

## freshbooks identity whoami

Show the full identity behind the current token

```
freshbooks identity whoami [flags]
```

### Options

```
  -h, --help   help for whoami
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks identity](#freshbooks-identity)	 - Manage identity

## freshbooks images

Manage images

### Options

```
  -h, --help   help for images
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks](#freshbooks)	 - A kubectl-style CLI for the FreshBooks REST API
* [freshbooks images upload](#freshbooks-images-upload)	 - Upload a logo or proposal image
* [freshbooks images upload-without-account](#freshbooks-images-upload-without-account)	 - Upload an image with no account association

## freshbooks images upload

Upload a logo or proposal image

```
freshbooks images upload [flags]
```

### Options

```
      --file string   path to the local file to upload (required)
  -h, --help          help for upload
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks images](#freshbooks-images)	 - Manage images

## freshbooks images upload-without-account

Upload an image with no account association

```
freshbooks images upload-without-account [flags]
```

### Options

```
      --file string   path to the local file to upload (required)
  -h, --help          help for upload-without-account
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks images](#freshbooks-images)	 - Manage images

## freshbooks invoice-profiles

Manage invoice-profiles

### Options

```
  -h, --help   help for invoice-profiles
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks](#freshbooks)	 - A kubectl-style CLI for the FreshBooks REST API
* [freshbooks invoice-profiles create](#freshbooks-invoice-profiles-create)	 - Create a recurring-invoice profile
* [freshbooks invoice-profiles delete](#freshbooks-invoice-profiles-delete)	 - Delete a recurring-invoice profile (destructive: requires --yes on a TTY)
* [freshbooks invoice-profiles enable-payment-options](#freshbooks-invoice-profiles-enable-payment-options)	 - Enable payment options on a recurring-invoice profile
* [freshbooks invoice-profiles get](#freshbooks-invoice-profiles-get)	 - Get a single recurring-invoice profile
* [freshbooks invoice-profiles list](#freshbooks-invoice-profiles-list)	 - List recurring-invoice profiles
* [freshbooks invoice-profiles update](#freshbooks-invoice-profiles-update)	 - Update a recurring-invoice profile

## freshbooks invoice-profiles create

Create a recurring-invoice profile

```
freshbooks invoice-profiles create [flags]
```

### Options

```
  -f, --file string           JSON request body: a file path, or - for stdin (required)
  -h, --help                  help for create
      --include stringArray   related sub-resource to embed in the response (repeatable)
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks invoice-profiles](#freshbooks-invoice-profiles)	 - Manage invoice-profiles

## freshbooks invoice-profiles delete

Delete a recurring-invoice profile (destructive: requires --yes on a TTY)

```
freshbooks invoice-profiles delete <id> [flags]
```

### Options

```
  -h, --help   help for delete
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks invoice-profiles](#freshbooks-invoice-profiles)	 - Manage invoice-profiles

## freshbooks invoice-profiles enable-payment-options

Enable payment options on a recurring-invoice profile

```
freshbooks invoice-profiles enable-payment-options <id> [flags]
```

### Options

```
  -f, --file string   JSON request body: a file path, or - for stdin (required)
  -h, --help          help for enable-payment-options
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks invoice-profiles](#freshbooks-invoice-profiles)	 - Manage invoice-profiles

## freshbooks invoice-profiles get

Get a single recurring-invoice profile

```
freshbooks invoice-profiles get <id> [flags]
```

### Options

```
  -h, --help                  help for get
      --include stringArray   related sub-resource to embed in the response (repeatable)
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks invoice-profiles](#freshbooks-invoice-profiles)	 - Manage invoice-profiles

## freshbooks invoice-profiles list

List recurring-invoice profiles

```
freshbooks invoice-profiles list [flags]
```

### Options

```
      --all                     walk every page and return every item (rejects --page/--per-page)
  -h, --help                    help for list
      --page int                1-based page number
      --per-page int            page size
      --search stringToString   filter as key=value (repeatable) (default [])
      --sort string             sort as field[:asc|desc] (default asc; direction encoding for business-scoped resources is unconfirmed against the live API -- see docs/progress.md)
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks invoice-profiles](#freshbooks-invoice-profiles)	 - Manage invoice-profiles

## freshbooks invoice-profiles update

Update a recurring-invoice profile

```
freshbooks invoice-profiles update <id> [flags]
```

### Options

```
  -f, --file string   JSON request body: a file path, or - for stdin (required)
  -h, --help          help for update
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks invoice-profiles](#freshbooks-invoice-profiles)	 - Manage invoice-profiles

## freshbooks invoices

Manage invoices

### Options

```
  -h, --help   help for invoices
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks](#freshbooks)	 - A kubectl-style CLI for the FreshBooks REST API
* [freshbooks invoices create](#freshbooks-invoices-create)	 - Create an invoice
* [freshbooks invoices delete](#freshbooks-invoices-delete)	 - Delete an invoice (destructive: requires --yes on a TTY)
* [freshbooks invoices enable-payment-options](#freshbooks-invoices-enable-payment-options)	 - Enable payment options on an invoice
* [freshbooks invoices get](#freshbooks-invoices-get)	 - Get a single invoice
* [freshbooks invoices invoice-presentation-defaults](#freshbooks-invoices-invoice-presentation-defaults)	 - Get an account's default invoice presentation styles
* [freshbooks invoices list](#freshbooks-invoices-list)	 - List invoices
* [freshbooks invoices pdf](#freshbooks-invoices-pdf)	 - Download an invoice as a PDF
* [freshbooks invoices send](#freshbooks-invoices-send)	 - Send an invoice by email
* [freshbooks invoices share-link](#freshbooks-invoices-share-link)	 - Get an invoice's public share link
* [freshbooks invoices update](#freshbooks-invoices-update)	 - Update an invoice

## freshbooks invoices create

Create an invoice

```
freshbooks invoices create [flags]
```

### Options

```
  -f, --file string           JSON request body: a file path, or - for stdin (required)
  -h, --help                  help for create
      --include stringArray   related sub-resource to embed in the response (repeatable)
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks invoices](#freshbooks-invoices)	 - Manage invoices

## freshbooks invoices delete

Delete an invoice (destructive: requires --yes on a TTY)

```
freshbooks invoices delete <id> [flags]
```

### Options

```
  -h, --help   help for delete
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks invoices](#freshbooks-invoices)	 - Manage invoices

## freshbooks invoices enable-payment-options

Enable payment options on an invoice

```
freshbooks invoices enable-payment-options <id> [flags]
```

### Options

```
  -f, --file string   JSON request body: a file path, or - for stdin (required)
  -h, --help          help for enable-payment-options
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks invoices](#freshbooks-invoices)	 - Manage invoices

## freshbooks invoices get

Get a single invoice

```
freshbooks invoices get <id> [flags]
```

### Options

```
  -h, --help                  help for get
      --include stringArray   related sub-resource to embed in the response (repeatable)
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks invoices](#freshbooks-invoices)	 - Manage invoices

## freshbooks invoices invoice-presentation-defaults

Get an account's default invoice presentation styles

```
freshbooks invoices invoice-presentation-defaults [flags]
```

### Options

```
  -h, --help   help for invoice-presentation-defaults
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks invoices](#freshbooks-invoices)	 - Manage invoices

## freshbooks invoices list

List invoices

```
freshbooks invoices list [flags]
```

### Options

```
      --all                     walk every page and return every item (rejects --page/--per-page)
  -h, --help                    help for list
      --page int                1-based page number
      --per-page int            page size
      --search stringToString   filter as key=value (repeatable) (default [])
      --sort string             sort as field[:asc|desc] (default asc; direction encoding for business-scoped resources is unconfirmed against the live API -- see docs/progress.md)
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks invoices](#freshbooks-invoices)	 - Manage invoices

## freshbooks invoices pdf

Download an invoice as a PDF

```
freshbooks invoices pdf <id> [flags]
```

### Options

```
      --force           overwrite an existing -o/--output file
  -h, --help            help for pdf
  -o, --output string   write the result to this file, or - for stdout (shadows the global -o/--output format flag on this command); refuses to overwrite an existing file without --force, and refuses - on a TTY (default "-")
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks invoices](#freshbooks-invoices)	 - Manage invoices

## freshbooks invoices send

Send an invoice by email

```
freshbooks invoices send <id> [flags]
```

### Options

```
  -f, --file string   JSON request body: a file path, or - for stdin (required)
  -h, --help          help for send
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks invoices](#freshbooks-invoices)	 - Manage invoices

## freshbooks invoices share-link

Get an invoice's public share link

```
freshbooks invoices share-link <id> [flags]
```

### Options

```
  -h, --help   help for share-link
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks invoices](#freshbooks-invoices)	 - Manage invoices

## freshbooks invoices update

Update an invoice

```
freshbooks invoices update <id> [flags]
```

### Options

```
  -f, --file string           JSON request body: a file path, or - for stdin (required)
  -h, --help                  help for update
      --include stringArray   related sub-resource to embed in the response (repeatable)
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks invoices](#freshbooks-invoices)	 - Manage invoices

## freshbooks items

Manage items

### Options

```
  -h, --help   help for items
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks](#freshbooks)	 - A kubectl-style CLI for the FreshBooks REST API
* [freshbooks items create](#freshbooks-items-create)	 - Create a catalogue item
* [freshbooks items delete](#freshbooks-items-delete)	 - Delete a catalogue item (destructive: requires --yes on a TTY)
* [freshbooks items get](#freshbooks-items-get)	 - Get a single catalogue item
* [freshbooks items list](#freshbooks-items-list)	 - List catalogue items
* [freshbooks items update](#freshbooks-items-update)	 - Update a catalogue item

## freshbooks items create

Create a catalogue item

```
freshbooks items create [flags]
```

### Options

```
  -f, --file string   JSON request body: a file path, or - for stdin (required)
  -h, --help          help for create
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks items](#freshbooks-items)	 - Manage items

## freshbooks items delete

Delete a catalogue item (destructive: requires --yes on a TTY)

```
freshbooks items delete <id> [flags]
```

### Options

```
  -h, --help   help for delete
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks items](#freshbooks-items)	 - Manage items

## freshbooks items get

Get a single catalogue item

```
freshbooks items get <id> [flags]
```

### Options

```
  -h, --help   help for get
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks items](#freshbooks-items)	 - Manage items

## freshbooks items list

List catalogue items

```
freshbooks items list [flags]
```

### Options

```
      --all                     walk every page and return every item (rejects --page/--per-page)
  -h, --help                    help for list
      --page int                1-based page number
      --per-page int            page size
      --search stringToString   filter as key=value (repeatable) (default [])
      --sort string             sort as field[:asc|desc] (default asc; direction encoding for business-scoped resources is unconfirmed against the live API -- see docs/progress.md)
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks items](#freshbooks-items)	 - Manage items

## freshbooks items update

Update a catalogue item

```
freshbooks items update <id> [flags]
```

### Options

```
  -f, --file string   JSON request body: a file path, or - for stdin (required)
  -h, --help          help for update
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks items](#freshbooks-items)	 - Manage items

## freshbooks journal-entries

Manage journal-entries

### Options

```
  -h, --help   help for journal-entries
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks](#freshbooks)	 - A kubectl-style CLI for the FreshBooks REST API
* [freshbooks journal-entries create](#freshbooks-journal-entries-create)	 - Add a journal entry
* [freshbooks journal-entries details](#freshbooks-journal-entries-details)	 - Get journal entry details

## freshbooks journal-entries create

Add a journal entry

```
freshbooks journal-entries create [flags]
```

### Options

```
  -f, --file string   JSON request body: a file path, or - for stdin (required)
  -h, --help          help for create
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks journal-entries](#freshbooks-journal-entries)	 - Manage journal-entries

## freshbooks journal-entries details

Get journal entry details

```
freshbooks journal-entries details [flags]
```

### Options

```
  -h, --help   help for details
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks journal-entries](#freshbooks-journal-entries)	 - Manage journal-entries

## freshbooks journal-entry-accounts

Manage journal-entry-accounts

### Options

```
  -h, --help   help for journal-entry-accounts
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks](#freshbooks)	 - A kubectl-style CLI for the FreshBooks REST API
* [freshbooks journal-entry-accounts list](#freshbooks-journal-entry-accounts-list)	 - List journal-entry accounts

## freshbooks journal-entry-accounts list

List journal-entry accounts

```
freshbooks journal-entry-accounts list [flags]
```

### Options

```
  -h, --help                    help for list
      --page int                1-based page number
      --per-page int            page size
      --search stringToString   filter as key=value (repeatable) (default [])
      --sort string             sort as field[:asc|desc] (default asc; direction encoding for business-scoped resources is unconfirmed against the live API -- see docs/progress.md)
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks journal-entry-accounts](#freshbooks-journal-entry-accounts)	 - Manage journal-entry-accounts

## freshbooks ledger-accounts

Manage ledger-accounts

### Options

```
  -h, --help   help for ledger-accounts
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks](#freshbooks)	 - A kubectl-style CLI for the FreshBooks REST API
* [freshbooks ledger-accounts create](#freshbooks-ledger-accounts-create)	 - Create a ledger account
* [freshbooks ledger-accounts get](#freshbooks-ledger-accounts-get)	 - Get a single ledger account
* [freshbooks ledger-accounts list](#freshbooks-ledger-accounts-list)	 - List ledger accounts
* [freshbooks ledger-accounts sub-type](#freshbooks-ledger-accounts-sub-type)	 - Get a single ledger account sub-type
* [freshbooks ledger-accounts sub-types](#freshbooks-ledger-accounts-sub-types)	 - List ledger account sub-types
* [freshbooks ledger-accounts types](#freshbooks-ledger-accounts-types)	 - List ledger account types
* [freshbooks ledger-accounts update](#freshbooks-ledger-accounts-update)	 - Update a ledger account

## freshbooks ledger-accounts create

Create a ledger account

```
freshbooks ledger-accounts create [flags]
```

### Options

```
  -f, --file string   JSON request body: a file path, or - for stdin (required)
  -h, --help          help for create
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks ledger-accounts](#freshbooks-ledger-accounts)	 - Manage ledger-accounts

## freshbooks ledger-accounts get

Get a single ledger account

```
freshbooks ledger-accounts get <account-uuid> [flags]
```

### Options

```
  -h, --help   help for get
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks ledger-accounts](#freshbooks-ledger-accounts)	 - Manage ledger-accounts

## freshbooks ledger-accounts list

List ledger accounts

```
freshbooks ledger-accounts list [flags]
```

### Options

```
  -h, --help   help for list
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks ledger-accounts](#freshbooks-ledger-accounts)	 - Manage ledger-accounts

## freshbooks ledger-accounts sub-type

Get a single ledger account sub-type

```
freshbooks ledger-accounts sub-type <id> [flags]
```

### Options

```
  -h, --help   help for sub-type
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks ledger-accounts](#freshbooks-ledger-accounts)	 - Manage ledger-accounts

## freshbooks ledger-accounts sub-types

List ledger account sub-types

```
freshbooks ledger-accounts sub-types [flags]
```

### Options

```
  -h, --help   help for sub-types
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks ledger-accounts](#freshbooks-ledger-accounts)	 - Manage ledger-accounts

## freshbooks ledger-accounts types

List ledger account types

```
freshbooks ledger-accounts types [flags]
```

### Options

```
  -h, --help   help for types
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks ledger-accounts](#freshbooks-ledger-accounts)	 - Manage ledger-accounts

## freshbooks ledger-accounts update

Update a ledger account

```
freshbooks ledger-accounts update <account-uuid> [flags]
```

### Options

```
  -f, --file string   JSON request body: a file path, or - for stdin (required)
  -h, --help          help for update
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks ledger-accounts](#freshbooks-ledger-accounts)	 - Manage ledger-accounts

## freshbooks other-income

Manage other-income

### Options

```
  -h, --help   help for other-income
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks](#freshbooks)	 - A kubectl-style CLI for the FreshBooks REST API
* [freshbooks other-income create](#freshbooks-other-income-create)	 - Record other income
* [freshbooks other-income delete](#freshbooks-other-income-delete)	 - Delete other income (destructive: requires --yes on a TTY)
* [freshbooks other-income list](#freshbooks-other-income-list)	 - List other income
* [freshbooks other-income update](#freshbooks-other-income-update)	 - Update other income

## freshbooks other-income create

Record other income

```
freshbooks other-income create [flags]
```

### Options

```
  -f, --file string   JSON request body: a file path, or - for stdin (required)
  -h, --help          help for create
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks other-income](#freshbooks-other-income)	 - Manage other-income

## freshbooks other-income delete

Delete other income (destructive: requires --yes on a TTY)

```
freshbooks other-income delete <id> [flags]
```

### Options

```
  -h, --help   help for delete
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks other-income](#freshbooks-other-income)	 - Manage other-income

## freshbooks other-income list

List other income

```
freshbooks other-income list [flags]
```

### Options

```
      --all                     walk every page and return every item (rejects --page/--per-page)
  -h, --help                    help for list
      --page int                1-based page number
      --per-page int            page size
      --search stringToString   filter as key=value (repeatable) (default [])
      --sort string             sort as field[:asc|desc] (default asc; direction encoding for business-scoped resources is unconfirmed against the live API -- see docs/progress.md)
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks other-income](#freshbooks-other-income)	 - Manage other-income

## freshbooks other-income update

Update other income

```
freshbooks other-income update <id> [flags]
```

### Options

```
  -f, --file string   JSON request body: a file path, or - for stdin (required)
  -h, --help          help for update
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks other-income](#freshbooks-other-income)	 - Manage other-income

## freshbooks payment-options

Manage payment-options

### Options

```
  -h, --help   help for payment-options
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks](#freshbooks)	 - A kubectl-style CLI for the FreshBooks REST API
* [freshbooks payment-options fb-pay-tokenize](#freshbooks-payment-options-fb-pay-tokenize)	 - Tokenize a card via FreshBooks Payments
* [freshbooks payment-options save-credit-card](#freshbooks-payment-options-save-credit-card)	 - Save a tokenized credit card to a recurring profile
* [freshbooks payment-options stripe-create-setup-intent](#freshbooks-payment-options-stripe-create-setup-intent)	 - Create a Stripe setup intent for a tokenized payment method
* [freshbooks payment-options stripe-tokenize](#freshbooks-payment-options-stripe-tokenize)	 - Tokenize a card via Stripe

## freshbooks payment-options fb-pay-tokenize

Tokenize a card via FreshBooks Payments

```
freshbooks payment-options fb-pay-tokenize [flags]
```

### Options

```
  -f, --file string   JSON request body: a file path, or - for stdin (required)
  -h, --help          help for fb-pay-tokenize
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks payment-options](#freshbooks-payment-options)	 - Manage payment-options

## freshbooks payment-options save-credit-card

Save a tokenized credit card to a recurring profile

```
freshbooks payment-options save-credit-card [flags]
```

### Options

```
  -f, --file string   JSON request body: a file path, or - for stdin (required)
  -h, --help          help for save-credit-card
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks payment-options](#freshbooks-payment-options)	 - Manage payment-options

## freshbooks payment-options stripe-create-setup-intent

Create a Stripe setup intent for a tokenized payment method

```
freshbooks payment-options stripe-create-setup-intent [flags]
```

### Options

```
  -h, --help                    help for stripe-create-setup-intent
      --payment-method string   the tokenized Stripe payment method key (required)
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks payment-options](#freshbooks-payment-options)	 - Manage payment-options

## freshbooks payment-options stripe-tokenize

Tokenize a card via Stripe

```
freshbooks payment-options stripe-tokenize [flags]
```

### Options

```
  -f, --file string   JSON request body: a file path, or - for stdin (required)
  -h, --help          help for stripe-tokenize
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks payment-options](#freshbooks-payment-options)	 - Manage payment-options

## freshbooks payments

Manage payments

### Options

```
  -h, --help   help for payments
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks](#freshbooks)	 - A kubectl-style CLI for the FreshBooks REST API
* [freshbooks payments create](#freshbooks-payments-create)	 - Record a payment against an invoice
* [freshbooks payments create-checkout-link](#freshbooks-payments-create-checkout-link)	 - Create a checkout link
* [freshbooks payments delete](#freshbooks-payments-delete)	 - Delete a payment (destructive: requires --yes on a TTY)
* [freshbooks payments delete-checkout-link](#freshbooks-payments-delete-checkout-link)	 - Delete a checkout link (destructive: requires --yes on a TTY)
* [freshbooks payments get](#freshbooks-payments-get)	 - Get a single payment
* [freshbooks payments list](#freshbooks-payments-list)	 - List invoice payments
* [freshbooks payments update](#freshbooks-payments-update)	 - Update a payment
* [freshbooks payments update-checkout-link](#freshbooks-payments-update-checkout-link)	 - Update a checkout link
* [freshbooks payments update-checkout-link-gateway](#freshbooks-payments-update-checkout-link-gateway)	 - Update a checkout link's payment gateway

## freshbooks payments create

Record a payment against an invoice

```
freshbooks payments create [flags]
```

### Options

```
  -f, --file string   JSON request body: a file path, or - for stdin (required)
  -h, --help          help for create
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks payments](#freshbooks-payments)	 - Manage payments

## freshbooks payments create-checkout-link

Create a checkout link

```
freshbooks payments create-checkout-link [flags]
```

### Options

```
  -f, --file string   JSON request body: a file path, or - for stdin (required)
  -h, --help          help for create-checkout-link
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks payments](#freshbooks-payments)	 - Manage payments

## freshbooks payments delete

Delete a payment (destructive: requires --yes on a TTY)

```
freshbooks payments delete <id> [flags]
```

### Options

```
  -h, --help   help for delete
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks payments](#freshbooks-payments)	 - Manage payments

## freshbooks payments delete-checkout-link

Delete a checkout link (destructive: requires --yes on a TTY)

```
freshbooks payments delete-checkout-link <id> [flags]
```

### Options

```
  -h, --help   help for delete-checkout-link
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks payments](#freshbooks-payments)	 - Manage payments

## freshbooks payments get

Get a single payment

```
freshbooks payments get <id> [flags]
```

### Options

```
  -h, --help   help for get
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks payments](#freshbooks-payments)	 - Manage payments

## freshbooks payments list

List invoice payments

```
freshbooks payments list [flags]
```

### Options

```
      --all                     walk every page and return every item (rejects --page/--per-page)
  -h, --help                    help for list
      --page int                1-based page number
      --per-page int            page size
      --search stringToString   filter as key=value (repeatable) (default [])
      --sort string             sort as field[:asc|desc] (default asc; direction encoding for business-scoped resources is unconfirmed against the live API -- see docs/progress.md)
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks payments](#freshbooks-payments)	 - Manage payments

## freshbooks payments update

Update a payment

```
freshbooks payments update <id> [flags]
```

### Options

```
  -f, --file string   JSON request body: a file path, or - for stdin (required)
  -h, --help          help for update
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks payments](#freshbooks-payments)	 - Manage payments

## freshbooks payments update-checkout-link

Update a checkout link

```
freshbooks payments update-checkout-link <id> [flags]
```

### Options

```
  -f, --file string   JSON request body: a file path, or - for stdin (required)
  -h, --help          help for update-checkout-link
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks payments](#freshbooks-payments)	 - Manage payments

## freshbooks payments update-checkout-link-gateway

Update a checkout link's payment gateway

```
freshbooks payments update-checkout-link-gateway <id> [flags]
```

### Options

```
  -f, --file string   JSON request body: a file path, or - for stdin (required)
  -h, --help          help for update-checkout-link-gateway
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks payments](#freshbooks-payments)	 - Manage payments

## freshbooks projects

Manage projects

### Options

```
  -h, --help   help for projects
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks](#freshbooks)	 - A kubectl-style CLI for the FreshBooks REST API
* [freshbooks projects abilities](#freshbooks-projects-abilities)	 - List a business's project abilities
* [freshbooks projects add-thread-comment](#freshbooks-projects-add-thread-comment)	 - Add a comment to a project discussion message
* [freshbooks projects create](#freshbooks-projects-create)	 - Create a project
* [freshbooks projects create-thread](#freshbooks-projects-create-thread)	 - Post a new message in a project's discussion
* [freshbooks projects delete](#freshbooks-projects-delete)	 - Delete a project (destructive: requires --yes on a TTY)
* [freshbooks projects get](#freshbooks-projects-get)	 - Get a single project
* [freshbooks projects list](#freshbooks-projects-list)	 - List projects
* [freshbooks projects threads](#freshbooks-projects-threads)	 - List a project's discussion messages
* [freshbooks projects update](#freshbooks-projects-update)	 - Update a project

## freshbooks projects abilities

List a business's project abilities

```
freshbooks projects abilities [flags]
```

### Options

```
  -h, --help   help for abilities
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks projects](#freshbooks-projects)	 - Manage projects

## freshbooks projects add-thread-comment

Add a comment to a project discussion message

```
freshbooks projects add-thread-comment <thread-id> [flags]
```

### Options

```
  -h, --help             help for add-thread-comment
      --message string   the comment text (required)
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks projects](#freshbooks-projects)	 - Manage projects

## freshbooks projects create

Create a project

```
freshbooks projects create [flags]
```

### Options

```
  -f, --file string   JSON request body: a file path, or - for stdin (required)
  -h, --help          help for create
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks projects](#freshbooks-projects)	 - Manage projects

## freshbooks projects create-thread

Post a new message in a project's discussion

```
freshbooks projects create-thread <id> [flags]
```

### Options

```
  -h, --help             help for create-thread
      --message string   the message text (required)
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks projects](#freshbooks-projects)	 - Manage projects

## freshbooks projects delete

Delete a project (destructive: requires --yes on a TTY)

```
freshbooks projects delete <id> [flags]
```

### Options

```
  -h, --help   help for delete
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks projects](#freshbooks-projects)	 - Manage projects

## freshbooks projects get

Get a single project

```
freshbooks projects get <id> [flags]
```

### Options

```
  -h, --help   help for get
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks projects](#freshbooks-projects)	 - Manage projects

## freshbooks projects list

List projects

```
freshbooks projects list [flags]
```

### Options

```
      --all                     walk every page and return every item (rejects --page/--per-page)
  -h, --help                    help for list
      --page int                1-based page number
      --per-page int            page size
      --search stringToString   filter as key=value (repeatable) (default [])
      --sort string             sort as field[:asc|desc] (default asc; direction encoding for business-scoped resources is unconfirmed against the live API -- see docs/progress.md)
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks projects](#freshbooks-projects)	 - Manage projects

## freshbooks projects threads

List a project's discussion messages

```
freshbooks projects threads <id> [flags]
```

### Options

```
  -h, --help   help for threads
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks projects](#freshbooks-projects)	 - Manage projects

## freshbooks projects update

Update a project

```
freshbooks projects update <id> [flags]
```

### Options

```
  -f, --file string   JSON request body: a file path, or - for stdin (required)
  -h, --help          help for update
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks projects](#freshbooks-projects)	 - Manage projects

## freshbooks reports

Manage reports

### Options

```
  -h, --help   help for reports
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks](#freshbooks)	 - A kubectl-style CLI for the FreshBooks REST API
* [freshbooks reports accounts-aging](#freshbooks-reports-accounts-aging)	 - Get the accounts aging report
* [freshbooks reports balance-sheet](#freshbooks-reports-balance-sheet)	 - Get the balance sheet report
* [freshbooks reports bank-reconciliation-summary](#freshbooks-reports-bank-reconciliation-summary)	 - Get the bank reconciliation summary report
* [freshbooks reports client-account-statement](#freshbooks-reports-client-account-statement)	 - Get a client's account statement
* [freshbooks reports download-invoice-details-csv](#freshbooks-reports-download-invoice-details-csv)	 - Download a previously requested invoice-details CSV export
* [freshbooks reports expense-details](#freshbooks-reports-expense-details)	 - Get the expense details report
* [freshbooks reports invoice-details](#freshbooks-reports-invoice-details)	 - Get the invoice details report
* [freshbooks reports item-sales](#freshbooks-reports-item-sales)	 - Get the item sales report
* [freshbooks reports payments-collected](#freshbooks-reports-payments-collected)	 - Get the payments collected report
* [freshbooks reports profit-loss](#freshbooks-reports-profit-loss)	 - Get the profit/loss report
* [freshbooks reports revenue-by-client](#freshbooks-reports-revenue-by-client)	 - Get the revenue by client report
* [freshbooks reports sales-tax-summary](#freshbooks-reports-sales-tax-summary)	 - Get the sales tax summary report
* [freshbooks reports time-entry-details](#freshbooks-reports-time-entry-details)	 - Get the time entry details report
* [freshbooks reports trial-balance](#freshbooks-reports-trial-balance)	 - Get the trial balance report

## freshbooks reports accounts-aging

Get the accounts aging report

```
freshbooks reports accounts-aging [flags]
```

### Options

```
  -f, --file string   JSON filter options: a file path, or - for stdin (optional)
  -h, --help          help for accounts-aging
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks reports](#freshbooks-reports)	 - Manage reports

## freshbooks reports balance-sheet

Get the balance sheet report

```
freshbooks reports balance-sheet [flags]
```

### Options

```
  -f, --file string   JSON filter options: a file path, or - for stdin (optional)
  -h, --help          help for balance-sheet
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks reports](#freshbooks-reports)	 - Manage reports

## freshbooks reports bank-reconciliation-summary

Get the bank reconciliation summary report

```
freshbooks reports bank-reconciliation-summary [flags]
```

### Options

```
  -f, --file string   JSON filter options: a file path, or - for stdin (optional)
  -h, --help          help for bank-reconciliation-summary
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks reports](#freshbooks-reports)	 - Manage reports

## freshbooks reports client-account-statement

Get a client's account statement

```
freshbooks reports client-account-statement [flags]
```

### Options

```
  -f, --file string   JSON filter options: a file path, or - for stdin (optional)
  -h, --help          help for client-account-statement
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks reports](#freshbooks-reports)	 - Manage reports

## freshbooks reports download-invoice-details-csv

Download a previously requested invoice-details CSV export

```
freshbooks reports download-invoice-details-csv <download-token> [flags]
```

### Options

```
      --force           overwrite an existing -o/--output file
  -h, --help            help for download-invoice-details-csv
  -o, --output string   write the result to this file, or - for stdout (shadows the global -o/--output format flag on this command); refuses to overwrite an existing file without --force, and refuses - on a TTY (default "-")
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks reports](#freshbooks-reports)	 - Manage reports

## freshbooks reports expense-details

Get the expense details report

```
freshbooks reports expense-details [flags]
```

### Options

```
  -f, --file string   JSON filter options: a file path, or - for stdin (optional)
  -h, --help          help for expense-details
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks reports](#freshbooks-reports)	 - Manage reports

## freshbooks reports invoice-details

Get the invoice details report

```
freshbooks reports invoice-details [flags]
```

### Options

```
  -f, --file string   JSON filter options: a file path, or - for stdin (optional)
  -h, --help          help for invoice-details
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks reports](#freshbooks-reports)	 - Manage reports

## freshbooks reports item-sales

Get the item sales report

```
freshbooks reports item-sales [flags]
```

### Options

```
  -f, --file string   JSON filter options: a file path, or - for stdin (optional)
  -h, --help          help for item-sales
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks reports](#freshbooks-reports)	 - Manage reports

## freshbooks reports payments-collected

Get the payments collected report

```
freshbooks reports payments-collected [flags]
```

### Options

```
  -f, --file string   JSON filter options: a file path, or - for stdin (optional)
  -h, --help          help for payments-collected
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks reports](#freshbooks-reports)	 - Manage reports

## freshbooks reports profit-loss

Get the profit/loss report

```
freshbooks reports profit-loss [flags]
```

### Options

```
  -f, --file string   JSON filter options: a file path, or - for stdin (optional)
  -h, --help          help for profit-loss
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks reports](#freshbooks-reports)	 - Manage reports

## freshbooks reports revenue-by-client

Get the revenue by client report

```
freshbooks reports revenue-by-client [flags]
```

### Options

```
  -f, --file string   JSON filter options: a file path, or - for stdin (optional)
  -h, --help          help for revenue-by-client
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks reports](#freshbooks-reports)	 - Manage reports

## freshbooks reports sales-tax-summary

Get the sales tax summary report

```
freshbooks reports sales-tax-summary [flags]
```

### Options

```
  -f, --file string   JSON filter options: a file path, or - for stdin (optional)
  -h, --help          help for sales-tax-summary
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks reports](#freshbooks-reports)	 - Manage reports

## freshbooks reports time-entry-details

Get the time entry details report

```
freshbooks reports time-entry-details [flags]
```

### Options

```
  -h, --help   help for time-entry-details
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks reports](#freshbooks-reports)	 - Manage reports

## freshbooks reports trial-balance

Get the trial balance report

```
freshbooks reports trial-balance [flags]
```

### Options

```
  -f, --file string   JSON filter options: a file path, or - for stdin (optional)
  -h, --help          help for trial-balance
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks reports](#freshbooks-reports)	 - Manage reports

## freshbooks retainers

Manage retainers

### Options

```
  -h, --help   help for retainers
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks](#freshbooks)	 - A kubectl-style CLI for the FreshBooks REST API
* [freshbooks retainers create](#freshbooks-retainers-create)	 - Create a retainer
* [freshbooks retainers delete](#freshbooks-retainers-delete)	 - Delete a retainer (destructive: requires --yes on a TTY)
* [freshbooks retainers get](#freshbooks-retainers-get)	 - Get a single retainer
* [freshbooks retainers list](#freshbooks-retainers-list)	 - List retainers
* [freshbooks retainers undelete](#freshbooks-retainers-undelete)	 - Undelete a retainer
* [freshbooks retainers update](#freshbooks-retainers-update)	 - Update a retainer

## freshbooks retainers create

Create a retainer

```
freshbooks retainers create [flags]
```

### Options

```
  -f, --file string   JSON request body: a file path, or - for stdin (required)
  -h, --help          help for create
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks retainers](#freshbooks-retainers)	 - Manage retainers

## freshbooks retainers delete

Delete a retainer (destructive: requires --yes on a TTY)

```
freshbooks retainers delete <id> [flags]
```

### Options

```
  -h, --help   help for delete
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks retainers](#freshbooks-retainers)	 - Manage retainers

## freshbooks retainers get

Get a single retainer

```
freshbooks retainers get <id> [flags]
```

### Options

```
  -h, --help   help for get
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks retainers](#freshbooks-retainers)	 - Manage retainers

## freshbooks retainers list

List retainers

```
freshbooks retainers list [flags]
```

### Options

```
  -h, --help                    help for list
      --search stringToString   filter as key=value (repeatable) (default [])
      --sort string             sort as field[:asc|desc] (default asc; direction encoding for business-scoped resources is unconfirmed against the live API -- see docs/progress.md)
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks retainers](#freshbooks-retainers)	 - Manage retainers

## freshbooks retainers undelete

Undelete a retainer

```
freshbooks retainers undelete <id> [flags]
```

### Options

```
  -h, --help   help for undelete
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks retainers](#freshbooks-retainers)	 - Manage retainers

## freshbooks retainers update

Update a retainer

```
freshbooks retainers update <id> [flags]
```

### Options

```
  -f, --file string   JSON request body: a file path, or - for stdin (required)
  -h, --help          help for update
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks retainers](#freshbooks-retainers)	 - Manage retainers

## freshbooks service-rates

Manage service-rates

### Options

```
  -h, --help   help for service-rates
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks](#freshbooks)	 - A kubectl-style CLI for the FreshBooks REST API
* [freshbooks service-rates get](#freshbooks-service-rates-get)	 - Get a single service rate
* [freshbooks service-rates list](#freshbooks-service-rates-list)	 - List service rates
* [freshbooks service-rates update-project-rate](#freshbooks-service-rates-update-project-rate)	 - Update a project's rate for a service

## freshbooks service-rates get

Get a single service rate

```
freshbooks service-rates get <service-id> [flags]
```

### Options

```
  -h, --help   help for get
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks service-rates](#freshbooks-service-rates)	 - Manage service-rates

## freshbooks service-rates list

List service rates

```
freshbooks service-rates list [flags]
```

### Options

```
  -h, --help   help for list
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks service-rates](#freshbooks-service-rates)	 - Manage service-rates

## freshbooks service-rates update-project-rate

Update a project's rate for a service

```
freshbooks service-rates update-project-rate <service-id> [flags]
```

### Options

```
  -h, --help             help for update-project-rate
      --project-id int   the project id (required)
      --rate string      the rate to set (required)
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks service-rates](#freshbooks-service-rates)	 - Manage service-rates

## freshbooks services

Manage services

### Options

```
  -h, --help   help for services
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks](#freshbooks)	 - A kubectl-style CLI for the FreshBooks REST API
* [freshbooks services create](#freshbooks-services-create)	 - Create a billable service
* [freshbooks services get](#freshbooks-services-get)	 - Get a single service
* [freshbooks services get-billable-item](#freshbooks-services-get-billable-item)	 - Get a single billable item
* [freshbooks services list](#freshbooks-services-list)	 - List services

## freshbooks services create

Create a billable service

```
freshbooks services create [flags]
```

### Options

```
  -f, --file string   JSON request body: a file path, or - for stdin (required)
  -h, --help          help for create
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks services](#freshbooks-services)	 - Manage services

## freshbooks services get

Get a single service

```
freshbooks services get <id> [flags]
```

### Options

```
  -h, --help   help for get
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks services](#freshbooks-services)	 - Manage services

## freshbooks services get-billable-item

Get a single billable item

```
freshbooks services get-billable-item <id> [flags]
```

### Options

```
  -h, --help   help for get-billable-item
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks services](#freshbooks-services)	 - Manage services

## freshbooks services list

List services

```
freshbooks services list [flags]
```

### Options

```
  -h, --help                    help for list
      --page int                1-based page number
      --per-page int            page size
      --search stringToString   filter as key=value (repeatable) (default [])
      --sort string             sort as field[:asc|desc] (default asc; direction encoding for business-scoped resources is unconfirmed against the live API -- see docs/progress.md)
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks services](#freshbooks-services)	 - Manage services

## freshbooks staff

Manage staff

### Options

```
  -h, --help   help for staff
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks](#freshbooks)	 - A kubectl-style CLI for the FreshBooks REST API
* [freshbooks staff delete](#freshbooks-staff-delete)	 - Delete a staff member (destructive: requires --yes on a TTY)
* [freshbooks staff get](#freshbooks-staff-get)	 - Get a single staff member
* [freshbooks staff list](#freshbooks-staff-list)	 - List a business's staff
* [freshbooks staff update](#freshbooks-staff-update)	 - Update a staff member

## freshbooks staff delete

Delete a staff member (destructive: requires --yes on a TTY)

```
freshbooks staff delete <id> [flags]
```

### Options

```
  -h, --help   help for delete
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks staff](#freshbooks-staff)	 - Manage staff

## freshbooks staff get

Get a single staff member

```
freshbooks staff get <id> [flags]
```

### Options

```
  -h, --help   help for get
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks staff](#freshbooks-staff)	 - Manage staff

## freshbooks staff list

List a business's staff

```
freshbooks staff list [flags]
```

### Options

```
  -h, --help   help for list
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks staff](#freshbooks-staff)	 - Manage staff

## freshbooks staff update

Update a staff member

```
freshbooks staff update <id> [flags]
```

### Options

```
  -f, --file string   JSON request body: a file path, or - for stdin (required)
  -h, --help          help for update
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks staff](#freshbooks-staff)	 - Manage staff

## freshbooks systems

Manage systems

### Options

```
  -h, --help   help for systems
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks](#freshbooks)	 - A kubectl-style CLI for the FreshBooks REST API
* [freshbooks systems get](#freshbooks-systems-get)	 - Get a business's system-level settings

## freshbooks systems get

Get a business's system-level settings

```
freshbooks systems get [flags]
```

### Options

```
  -h, --help   help for get
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks systems](#freshbooks-systems)	 - Manage systems

## freshbooks tasks

Manage tasks

### Options

```
  -h, --help   help for tasks
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks](#freshbooks)	 - A kubectl-style CLI for the FreshBooks REST API
* [freshbooks tasks create](#freshbooks-tasks-create)	 - Create a project task
* [freshbooks tasks delete](#freshbooks-tasks-delete)	 - Delete a task (destructive: requires --yes on a TTY)
* [freshbooks tasks get](#freshbooks-tasks-get)	 - Get a single task
* [freshbooks tasks list](#freshbooks-tasks-list)	 - List tasks
* [freshbooks tasks update](#freshbooks-tasks-update)	 - Update a task

## freshbooks tasks create

Create a project task

```
freshbooks tasks create [flags]
```

### Options

```
  -f, --file string   JSON request body: a file path, or - for stdin (required)
  -h, --help          help for create
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks tasks](#freshbooks-tasks)	 - Manage tasks

## freshbooks tasks delete

Delete a task (destructive: requires --yes on a TTY)

```
freshbooks tasks delete <id> [flags]
```

### Options

```
  -h, --help   help for delete
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks tasks](#freshbooks-tasks)	 - Manage tasks

## freshbooks tasks get

Get a single task

```
freshbooks tasks get <id> [flags]
```

### Options

```
  -h, --help   help for get
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks tasks](#freshbooks-tasks)	 - Manage tasks

## freshbooks tasks list

List tasks

```
freshbooks tasks list [flags]
```

### Options

```
      --all                     walk every page and return every item (rejects --page/--per-page)
  -h, --help                    help for list
      --page int                1-based page number
      --per-page int            page size
      --search stringToString   filter as key=value (repeatable) (default [])
      --sort string             sort as field[:asc|desc] (default asc; direction encoding for business-scoped resources is unconfirmed against the live API -- see docs/progress.md)
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks tasks](#freshbooks-tasks)	 - Manage tasks

## freshbooks tasks update

Update a task

```
freshbooks tasks update <id> [flags]
```

### Options

```
  -f, --file string   JSON request body: a file path, or - for stdin (required)
  -h, --help          help for update
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks tasks](#freshbooks-tasks)	 - Manage tasks

## freshbooks taxes

Manage taxes

### Options

```
  -h, --help   help for taxes
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks](#freshbooks)	 - A kubectl-style CLI for the FreshBooks REST API
* [freshbooks taxes create](#freshbooks-taxes-create)	 - Create a tax rate
* [freshbooks taxes delete](#freshbooks-taxes-delete)	 - Delete a tax rate (destructive: requires --yes on a TTY)
* [freshbooks taxes get](#freshbooks-taxes-get)	 - Get a single tax rate
* [freshbooks taxes list](#freshbooks-taxes-list)	 - List tax rates
* [freshbooks taxes update](#freshbooks-taxes-update)	 - Update a tax rate

## freshbooks taxes create

Create a tax rate

```
freshbooks taxes create [flags]
```

### Options

```
  -f, --file string   JSON request body: a file path, or - for stdin (required)
  -h, --help          help for create
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks taxes](#freshbooks-taxes)	 - Manage taxes

## freshbooks taxes delete

Delete a tax rate (destructive: requires --yes on a TTY)

```
freshbooks taxes delete <id> [flags]
```

### Options

```
  -h, --help   help for delete
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks taxes](#freshbooks-taxes)	 - Manage taxes

## freshbooks taxes get

Get a single tax rate

```
freshbooks taxes get <id> [flags]
```

### Options

```
  -h, --help                  help for get
      --include stringArray   related sub-resource to embed in the response (repeatable)
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks taxes](#freshbooks-taxes)	 - Manage taxes

## freshbooks taxes list

List tax rates

```
freshbooks taxes list [flags]
```

### Options

```
      --all                     walk every page and return every item (rejects --page/--per-page)
  -h, --help                    help for list
      --page int                1-based page number
      --per-page int            page size
      --search stringToString   filter as key=value (repeatable) (default [])
      --sort string             sort as field[:asc|desc] (default asc; direction encoding for business-scoped resources is unconfirmed against the live API -- see docs/progress.md)
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks taxes](#freshbooks-taxes)	 - Manage taxes

## freshbooks taxes update

Update a tax rate

```
freshbooks taxes update <id> [flags]
```

### Options

```
  -f, --file string   JSON request body: a file path, or - for stdin (required)
  -h, --help          help for update
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks taxes](#freshbooks-taxes)	 - Manage taxes

## freshbooks team-members

Manage team-members

### Options

```
  -h, --help   help for team-members
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks](#freshbooks)	 - A kubectl-style CLI for the FreshBooks REST API
* [freshbooks team-members get](#freshbooks-team-members-get)	 - Get a single team member
* [freshbooks team-members invitation-rates](#freshbooks-team-members-invitation-rates)	 - List a business's invitation rates
* [freshbooks team-members invite](#freshbooks-team-members-invite)	 - Invite a team member to one or more projects
* [freshbooks team-members list](#freshbooks-team-members-list)	 - List team members
* [freshbooks team-members rates](#freshbooks-team-members-rates)	 - List a business's team member rates
* [freshbooks team-members update-rate](#freshbooks-team-members-update-rate)	 - Update a team member's rate

## freshbooks team-members get

Get a single team member

```
freshbooks team-members get <team-member-uuid> [flags]
```

### Options

```
  -h, --help   help for get
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks team-members](#freshbooks-team-members)	 - Manage team-members

## freshbooks team-members invitation-rates

List a business's invitation rates

```
freshbooks team-members invitation-rates [flags]
```

### Options

```
  -h, --help   help for invitation-rates
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks team-members](#freshbooks-team-members)	 - Manage team-members

## freshbooks team-members invite

Invite a team member to one or more projects

```
freshbooks team-members invite [flags]
```

### Options

```
  -f, --file string   JSON request body: a file path, or - for stdin (required)
  -h, --help          help for invite
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks team-members](#freshbooks-team-members)	 - Manage team-members

## freshbooks team-members list

List team members

```
freshbooks team-members list [flags]
```

### Options

```
      --all                     walk every page and return every item (rejects --page/--per-page)
  -h, --help                    help for list
      --page int                1-based page number
      --per-page int            page size
      --search stringToString   filter as key=value (repeatable) (default [])
      --sort string             sort as field[:asc|desc] (default asc; direction encoding for business-scoped resources is unconfirmed against the live API -- see docs/progress.md)
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks team-members](#freshbooks-team-members)	 - Manage team-members

## freshbooks team-members rates

List a business's team member rates

```
freshbooks team-members rates [flags]
```

### Options

```
  -h, --help   help for rates
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks team-members](#freshbooks-team-members)	 - Manage team-members

## freshbooks team-members update-rate

Update a team member's rate

```
freshbooks team-members update-rate <identity-id> [flags]
```

### Options

```
  -h, --help          help for update-rate
      --rate string   the rate to set (required)
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks team-members](#freshbooks-team-members)	 - Manage team-members

## freshbooks time-entries

Manage time-entries

### Options

```
  -h, --help   help for time-entries
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks](#freshbooks)	 - A kubectl-style CLI for the FreshBooks REST API
* [freshbooks time-entries create](#freshbooks-time-entries-create)	 - Create a time entry
* [freshbooks time-entries delete](#freshbooks-time-entries-delete)	 - Delete a time entry (destructive: requires --yes on a TTY)
* [freshbooks time-entries list](#freshbooks-time-entries-list)	 - List time entries
* [freshbooks time-entries search](#freshbooks-time-entries-search)	 - Search time entries for an employee on a specific project
* [freshbooks time-entries update](#freshbooks-time-entries-update)	 - Update a time entry

## freshbooks time-entries create

Create a time entry

```
freshbooks time-entries create [flags]
```

### Options

```
  -f, --file string   JSON request body: a file path, or - for stdin (required)
  -h, --help          help for create
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks time-entries](#freshbooks-time-entries)	 - Manage time-entries

## freshbooks time-entries delete

Delete a time entry (destructive: requires --yes on a TTY)

```
freshbooks time-entries delete <id> [flags]
```

### Options

```
  -h, --help   help for delete
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks time-entries](#freshbooks-time-entries)	 - Manage time-entries

## freshbooks time-entries list

List time entries

```
freshbooks time-entries list [flags]
```

### Options

```
  -h, --help                    help for list
      --page int                1-based page number
      --per-page int            page size
      --search stringToString   filter as key=value (repeatable) (default [])
      --sort string             sort as field[:asc|desc] (default asc; direction encoding for business-scoped resources is unconfirmed against the live API -- see docs/progress.md)
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks time-entries](#freshbooks-time-entries)	 - Manage time-entries

## freshbooks time-entries search

Search time entries for an employee on a specific project

```
freshbooks time-entries search <query> [flags]
```

### Options

```
  -h, --help                    help for search
      --page int                1-based page number
      --per-page int            page size
      --search stringToString   filter as key=value (repeatable) (default [])
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks time-entries](#freshbooks-time-entries)	 - Manage time-entries

## freshbooks time-entries update

Update a time entry

```
freshbooks time-entries update <id> [flags]
```

### Options

```
  -f, --file string   JSON request body: a file path, or - for stdin (required)
  -h, --help          help for update
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks time-entries](#freshbooks-time-entries)	 - Manage time-entries

## freshbooks version

Print the freshbooks CLI version

```
freshbooks version [flags]
```

### Options

```
  -h, --help   help for version
```

### Options inherited from parent commands

```
      --account string         FreshBooks account id (accounting-family scope)
      --business string        FreshBooks business id (business-family scope)
      --business-uuid string   FreshBooks business UUID (ledger-accounts scope)
      --config string          path to config.yaml (default: $XDG_CONFIG_HOME/freshbooks/config.yaml, or ~/.config/freshbooks/config.yaml if $XDG_CONFIG_HOME is unset)
      --context string         config context to use (default: config.yaml's current-context, else "default")
      --dry-run                print the request that would be sent and send nothing
      --log-level string       log level: debug, info, warn, or error (default warn; env twin FRESHBOOKS_LOG_LEVEL)
      --no-headers             suppress the header row in table output
  -o, --output string          output format: json, yaml, table, or name (default: table on a terminal, json otherwise)
  -q, --quiet                  suppress non-result output (errors still print)
      --timeout duration       per-request timeout (default 30s)
      --yes                    confirm a destructive command
```

### SEE ALSO

* [freshbooks](#freshbooks)	 - A kubectl-style CLI for the FreshBooks REST API

