# Phase 10 implementer report

Branch `phase-10/docs-site`, 7 checkpoint commits (D1-D6; D7 hygiene folded into D2/D6, no separate commit needed -- see "D7" below) on top of `d73e122`. `mise run site-build` and the full `mise run check` are both green on a clean tree.

## Dependency tree

```
freshbooks-tools-site@0.0.0 <repo root>/site (PRIVATE)
dependencies:
  - @docusaurus/core@3.10.2
  - @docusaurus/preset-classic@3.10.2
  - react@19.2.8
  - react-dom@19.2.8

4 packages
```

`site/pnpm-lock.yaml` resolves 690 packages total (transitive). One allow-listed build script: `core-js@3.50.0` (transitive via `@docusaurus/core` -> `@docusaurus/plugin-content-blog`, a babel polyfill target), allow-listed in `site/pnpm-workspace.yaml`'s `allowBuilds: core-js: true`. Verified before allow-listing by reading `node_modules/core-js/postinstall.js`: it only prints a donation banner (checks `ADBLOCK`/`CI`/`DISABLE_OPENCOLLECTIVE` env vars and a `/tmp` rate-limit file, then `console.log`) -- no compilation, no native binary, no network call. No other packages needed a build-script allow-list.

`@docusaurus/theme-search-algolia` is present in the tree as an inherent, non-optional dependency of `@docusaurus/preset-classic` (bundled by the preset itself, not something we opted into) -- it stays inert since `themeConfig.algolia` is never configured, so "no search plugin" holds in effect even though the package sits unused in `node_modules`.

## First-install error and resolution

First `pnpm install` failed as expected under the machine-wide hardening: `ERR_PNPM_IGNORED_BUILDS: Ignored build scripts: core-js@3.50.0` (`strict-dep-builds=true`). Two follow-on surprises before it resolved:

1. `package.json`'s `"pnpm": { "onlyBuiltDependencies": [...] }` field is silently ignored by pnpm 11 with a warning -- that setting moved to `pnpm-workspace.yaml` (a single-package `site/` still needs this file just to carry the setting, no `packages:` list required).
2. Even in `pnpm-workspace.yaml`, `onlyBuiltDependencies` wasn't what unblocked it -- pnpm auto-wrote an `allowBuilds: { core-js: "set this to true or false" }` stub into that same file (a pnpm-11 feature under `strict-dep-builds`, distinct from `onlyBuiltDependencies`) that needed an explicit `true` after reading the script. Once set, install succeeded (`pnpm-lock.yaml` committed, `site/node_modules`/`site/build` gitignored).

No `dangerously-allow-all-builds`, no `minimum-release-age` relaxation. `react`/`react-dom` pinned at 19.2.8 (published 2026-07-21) and `@docusaurus/core`/`@docusaurus/preset-classic` at 3.10.2 (published 2026-07-10) -- both clear the 7-day gate.

## Build times

- Warm (webpack cache present): **~5.3s** (`time` on a bare `pnpm exec docusaurus build`), ~5-7s through the full `mise run site-build` task including sync + `pnpm install --frozen-lockfile`.
- Cold (after `pnpm exec docusaurus clear`): **~26.2s**.

Both comfortably under the 60s budget, so per D4 the build joins `scripts/check.sh`'s `run_repo_wide()` (and therefore `mise run check`), rather than running in CI only.

## Sidebar as rendered

Verified by building and grepping `site/build/getting-started.html`'s `menu__link` entries (an explicit list in `site/sidebars.js`, not autogenerated -- the plan's 7-page enumeration deliberately excludes `index`/home, which is the synced README.md landing page, not part of the guide sequence):

1. Getting started
2. Authentication
3. Library
4. MCP server
5. CLI reference
6. Building
7. Agentic transformation

## `pnpm --dir site build` tail (final green run, via `mise run site-build`)

```
$ docusaurus build
[INFO] [en] Creating an optimized production build...
[webpackbar] i Compiling Client
[webpackbar] i Compiling Server
[webpackbar] v Server: Compiled successfully in 1.84s
[webpackbar] v Client: Compiled successfully in 2.58s
[SUCCESS] Generated static files in "build".
[INFO] Use `npm run serve` command to test your build locally.
site-build: OK
```

(The `npm run serve` wording is Docusaurus's own hard-coded CLI message, not ours -- pnpm is still what actually runs everything.)

## Gate tail (`mise run check`, full run, clean tree, ~72s wall clock)

```
coverage-gate: <repo root>/freshbooks/coverage.out total = 91.9% (floor 90%)
coverage-gate: PASS
coverage-gate: <repo root>/mcp/coverage.out total = 92.1% (floor 90%)
coverage-gate: PASS
coverage-gate: <repo root>/cli/coverage.out total = 91.6% (floor 90%)
coverage-gate: PASS
...
== readme-drift-check ==
release: OK docs
== site-build ==
site-sync: regenerated site/docs/ (8 pages)
Already up to date
Done in 566ms using pnpm v11.15.1
$ docusaurus build
[webpackbar] v Server: Compiled successfully in 1.84s
[webpackbar] v Client: Compiled successfully in 2.58s
[SUCCESS] Generated static files in "build".
site-build: OK
check.sh: all OK
```

## `git log --oneline main..phase-10/docs-site`

```
7465088 docs: document the docs site, link it from README (D6)
57b591f ci(pages): add the GitHub Pages deploy workflow (D5)
16b06fd chore(mise): wire the docs site into mise tasks and the repo-wide gate (D4)
4d4871f fix(site): correct two D2 config decisions the real build disagreed with
bed2d5d chore(site): resolve pnpm install hardening, commit lockfile (D3)
6540a65 feat(site): scaffold Docusaurus 3.10.2 site (D2)
292d61b chore(site): add scripts/site-sync.sh (D1)
d73e122 docs(phase-10): add the docs-site plan and work order
```

## `git status --porcelain`

```
(clean)
```

## Where reality disagreed with D1-D7

- **D2's `format: md` front matter is stale spelling.** Docusaurus 3.10.2's `@docusaurus/mdx-loader` reads a doc's MDX-vs-CommonMark opt-out from front matter `mdx.format`, not a bare top-level `format` key -- `compileToJSX` calls `validateMDXFrontMatter(frontMatter.mdx)`. Without the nesting, `docs/cli.md`'s ``source <(freshbooks completion bash)`` line (inside an indented code block) was parsed as MDX-JSX and failed to compile with `Unexpected character '(' ... expected a character that can start a name`. Fixed in `scripts/site-sync.sh`: emits `mdx:\n  format: md`.
- **D2's `onBrokenMarkdownLinks` top-level key is deprecated** as of Docusaurus 3.10 (still works, but warns and will be removed in v4). Moved to `markdown.hooks.onBrokenMarkdownLinks` in `site/docusaurus.config.js`, same `'throw'` value.
- **Empty `site/static/` broke the build**, unrelated to any D-decision: a webpack CopyPlugin glob (`site/static/**/*`) errors when it matches zero files. Resolved by adding `site/static/.nojekyll` (real content -- GitHub Pages skips Jekyll processing of underscore-prefixed paths; also happens to be a no-op under `actions/upload-pages-artifact`, which never runs Jekyll at all, but it is the conventional file to ship regardless) and removing the leftover empty `static/img/`.
- **Discovered independently, documented rather than fixed (not a D-decision, a real Docusaurus/Node behavior):** `@docusaurus/core`'s `ForceTerminatePlugin` calls `process.exit(1)` synchronously right after logging the detailed webpack error on a client-bundle compile failure. Under a non-TTY stdout (any redirect to a file, `mise run site-build > log`, CI logs, or piping through `script`) Node can drop the tail of that write before the process dies, so a real build failure surfaces only as the generic "Client bundle compiled with errors therefore further build is impossible." with the actual error missing. Hit this directly while debugging the two fixes above -- diagnosed by requiring a small Node script via `NODE_OPTIONS="--require ..."` that delays `process.exit` by two `setImmediate` ticks, which let the real error print. Documented as a debugging note in `docs/building.md`'s new "Docs site" section; not patched into the gate itself (would mean shipping a patched `node_modules` behavior into a supply-chain-hardened install path for a readability-only concern, not a correctness one).
- Everything else in D1-D7 landed as specified: `site/` layout, Docusaurus config (docs-only, dark mode, no blog/search/telemetry/external scripts, system font stack), install hardening resolution, mise tasks + gate wiring, the Pages workflow (not enabled/deployed by this implementer, per the work order), and the doc updates.

No push, no Pages enablement, no deploy. `redaction-check.sh` ran clean before every commit; a final sweep of all tracked `site/` files and every file this phase touched found no operator strings and no non-ASCII characters.
