# Work order: Phase 5 implementer (release hardening)

Dispatch: `Agent(subagent_type: "general-purpose", model: "sonnet", name: "phase-5-impl")`.

Stage-1 findings (lead, 2026-09-02) are the decisions D1-D10 below. They were verified live: `gh api` for every action and tool version, goreleaser v2.18.0 source (`internal/pipe/git/git.go`, `internal/pipe/semver`, `internal/client/github.go`, `pkg/config`), and a scratch-clone dry run with a real `mcp/v0.1.0` tag. Do not re-open them; report if reality disagrees.

---

You are implementing **Phase 5 (release hardening)** of `freshbooks-tools`, a public MIT Go monorepo. Work ONLY inside `<repo root>` on branch `phase-5/release` (already checked out, clean; this work order is its first commit). Do not touch other branches. All `git`, `go`, and `mise` commands run from `<repo root>` or with `-C <module>`. Use `mise exec -- go ...` for any direct `go` invocation.

**Context budget rules (a 30-item fix order overflowed one context in Phase 4):** never `cat` a file over ~200 lines (use `grep -n`, `sed -n 'a,bp'`, `head`, `tail`); write every gate run to a log file and read only its tail (`mise run check > /tmp/check.log 2>&1; echo $?; tail -20 /tmp/check.log`); never pipe the gate through `tail`/`head` directly (masks the exit code); `docs/cli.md` is 8200 lines -- only ever touch its header through `docsgen` and regenerate. **Checkpoint commits are mandatory:** one conventional commit per fold F1-F10 below, each green for the module(s) it touches (`mise run check -- <module>` at minimum; the full gate before F10 and at the end).

## Read first (pointers, not pasted)

1. `CLAUDE.md` (toolchain, commits, green rule, public-repo hygiene, docs rules: ASCII-only, never hard-wrapped, `--` and `->` not em dashes/arrows). `GOAL.md` stage 2 (the deliverable list; this work order refines it).
2. The spec `docs/superpowers/specs/2026-08-22-freshbooks-tools-design.md` sections 8.2-8.5 (mise tasks, CI, release, docs) and 2 (locked: tags `freshbooks/vX.Y.Z`, `mcp/vX.Y.Z`, `cli/vX.Y.Z`; binaries `freshbooks`, `freshbooks-mcp`; docs are Markdown + pkg.go.dev).
3. `docs/progress.md` "Phase-close backlog" (the numbered items F4-F7 fold) and `docs/phases/4/reports/qa.md` Q4, Q12, Q13, Q15, Q16, Q17, Q18, Q20, Q21, Q22 (the exact findings F7 folds, with file:line evidence).
4. Release plumbing as it is now: `.github/workflows/{ci,release}.yml`, `mcp/.goreleaser.yaml`, `cli/.goreleaser.yaml`, `mise.toml`, `scripts/{check,build,docs,changelog-section}.sh`.
5. The CLI docs seam: `cli/internal/cmd/{docsgen.go,docs_cmd.go,docs_drift_test.go,docs_test.go}` and `cli/internal/cmd/root.go:44` where the hidden `docs` command is attached.
6. Version wiring: `cli/cmd/freshbooks/main.go`, `mcp/cmd/freshbooks-mcp/main.go` (each must stay one statement in `main()`), `cli/internal/cmd/root.go` (`Run`, `newVersionCmd`), `mcp/cmd/freshbooks-mcp/` `run()`.

## Stage-1 decisions you inherit

- **D1 -- goreleaser OSS cannot release a prefixed tag; goreleaser builds, `gh` publishes.** Verified: `pkg/config` has no `monorepo` key in OSS 2.18.0 (Pro-only still holds); with the real tag `mcp/v0.1.0` and `GORELEASER_CURRENT_TAG=v0.1.0`, `goreleaser release --skip=publish --clean` fails at `validate()` (`git tag v0.1.0 was not made against commit ...`) because it runs `git describe --exact-match --match v0.1.0`; and even if it passed, the GitHub release pipe creates the release on `ctx.Git.CurrentTag` (`internal/client/github.go`), i.e. on a new tag `v0.1.0`, not on `mcp/v0.1.0`. With `--skip=publish,validate` the same command succeeds in 21s and produces archives, `checksums.txt`, and SBOMs, and the binary prints `freshbooks-mcp 0.1.0`. The release job therefore becomes: `GORELEASER_CURRENT_TAG=v<version> goreleaser release --skip=publish,validate --clean` (goreleaser from `mise.toml`, no `goreleaser-action`), then `gh release create "$TAG_NAME" --verify-tag --notes-file <notes> dist/*.tar.gz dist/*.zip dist/checksums.txt dist/*.sbom.json`. `--skip=validate` also skips goreleaser's dirty-tree check; the CI checkout is pristine and `guard` already enforces semver + on-`main`, so nothing is lost -- say so in the workflow comment. The lib job keeps `gh release create` and gains `--verify-tag`.
- **D2 -- `project_name` per module, `GOWORK=off` release builds.** The dry run named every archive `freshbooks-tools_0.1.0_<os>_<arch>` (goreleaser defaults `project_name` to the git remote's repo name), so `mcp/` and `cli/` archives would collide. Set `project_name: freshbooks-mcp` and `project_name: freshbooks`. Set `builds[].env: [CGO_ENABLED=0, GOWORK=off]` and `mod_timestamp: "{{ .CommitTimestamp }}"` so a release binary is built from the module's own `go.mod` (the same lib pseudo-version a `go install ...@tag` user gets), not from the workspace's sibling checkout. Verified `GOWORK=off` builds succeed for both modules today. Replace the `NOTE:` comment in both `.goreleaser.yaml` files with a two-line one that states D1.
- **D3 -- syft via mise.** SBOM generation shells out to `syft`, which is on neither the dev machine nor the runner (dry run: `exec: "syft": executable file not found`). Add `"aqua:anchore/syft" = "1.51.1"` to `mise.toml [tools]` (the `mise registry` short name is `syft`; use the explicit aqua form so the backend is pinned). `jdx/mise-action` with `install: true` then provides it in every CI job.
- **D4 -- actions pinned by SHA, and fewer of them.** Current latest (all lightweight `commit` refs, verified with `gh api repos/<r>/git/ref/tags/<t>`): `actions/checkout` v7.0.1 = `3d3c42e5aac5ba805825da76410c181273ba90b1`; `jdx/mise-action` v4.3.0 = `c2a87611a18de5b3828c5652fe268e992400cb5c`. Use `uses: <owner>/<repo>@<sha> # v<version>` in both workflows. Drop `actions/upload-artifact` + `actions/download-artifact` entirely: the `release` job re-runs `scripts/changelog-section.sh` itself (mise is installed there anyway) instead of round-tripping `/tmp/release-notes.md` through an artifact; `guard` keeps running it as the early fail. Drop `goreleaser/goreleaser-action` per D1. After this, the only third-party actions are `checkout` and `mise-action`. Keep `permissions: contents: read` on `guard`/`ci` and `contents: write` on `release` only. `actionlint` must stay green.
- **D5 -- toolchain and dependency refresh.** `mise.toml`: `goreleaser = "2.18.0"`, `golangci-lint = "2.13.2"` (latest per `gh api .../releases/latest`); keep `go = "1.26.6"`, `actionlint`, `usage` as they are. Modules: `freshbooks` is already current (stdlib + testify); in `mcp/` and `cli/` run `mise exec -- go get -u ./... && mise exec -- go mod tidy` (in workspace mode, from inside the module) -- expected bumps: `pflag` 1.0.10, `x/sys` 0.47.0 (closes GO-2026-5024), `x/oauth2` 0.36.0, `x/sync` 0.22.0, `go-md2man` 2.0.7, `go.yaml.in/yaml/v3` 3.0.5, `segmentio/asm` 1.2.1, and the lib pseudo-version to `v0.0.0-20260902041524-dbd898b28413` (main HEAD). No new direct dependencies; if `go get -u` tries to add one, revert it and report. `go.work.sum` is tracked; commit its diff. `mise run vuln` must be clean. If the `golangci-lint` bump surfaces new findings, fix them; never widen `.golangci.yml` exclusions.
- **D6 -- cobra/doc leaves the release binary** (backlog item 2, security A6). Verified: `go version -m` on a freshly built `freshbooks` binary lists `go-md2man` and `blackfriday`; the `freshbooks-mcp` binary lists neither. Shape: new package `cli/internal/docsgen` holding the generator (`Generate(root *cobra.Command) ([]byte, error)` and the header text; it is the only non-test importer of `github.com/spf13/cobra/doc`); the hidden `docs` command moves to a file with `//go:build docsgen` that registers itself through a package-level hook slice in `cli/internal/cmd` (an untagged `var extraCommands []func(*cobra.Command) *cobra.Command` appended from the tagged file's `init()`, iterated in `NewRootCmd`); `scripts/docs.sh` runs `go run -tags docsgen ./cmd/freshbooks docs ...`; the drift test `docs_drift_test.go` stays UNTAGGED (so it keeps running in the default gate) and calls `docsgen.Generate` directly on a root that has been through the same side effects `Execute` applies (cobra adds `completion` during `ExecuteC`; reproduce it -- `root.InitDefaultCompletionCmd()` or run the tree once through `Run` with `--help` -- and prove byte-equality with `mise run docs` output); `scripts/check.sh` `run_test`'s second pass becomes `-tags integration,docsgen` so the tagged `docs` command and its tests (`docs_test.go`, tag it too) still compile and run in the gate. Acceptance: `mise exec -- go -C cli build -o /tmp/fb ./cmd/freshbooks && go version -m /tmp/fb | grep -c 'md2man\|blackfriday'` prints `0`; `mise run docs` is idempotent and `docs/cli.md` is byte-identical before and after this fold except for the F7 header edits (do F7's header edits in F7, not here, so this fold's `docs/cli.md` diff is empty); cli coverage stays >= 90%.
- **D7 -- `version` falls back to the module version.** A `go install .../cmd/freshbooks@v0.1.0` build has no ldflags, so today it prints `0.0.0-dev`. In the tested entry seam of each binary (`cli/internal/cmd.Run` or wherever `version` is first consumed, and the mcp `run()`), when `version == "0.0.0-dev"` and `debug.ReadBuildInfo()` reports a `Main.Version` that is neither empty nor `(devel)`, use it (a `go install @sha` user then sees the pseudo-version, a `@v0.1.0` user sees `v0.1.0`). Unit-test the substitution with an injected build-info reader (do not shell out to `go install` in a unit test); QA will do the real `go install` probe. `main.go` stays one statement.
- **D8 -- `scripts/check.sh` dirty-tree guard ignores `docs/phases/*/reports/`** (backlog item 7): `git status --porcelain -- . ':(exclude)docs/phases/*/reports'` (pathspec magic, no extra tooling). One-line change plus a one-line comment saying why (the QA lane writes its report while the gate runs). Note this in `docs/building.md`'s gate paragraph.
- **D9 -- Phase 4 QA advisories folded (one commit, `fix(cli): fold the Phase 4 QA advisories`)**: Q4 pin `len(auth.DefaultScopes) == 44` and assert two literal scope strings; Q12 rename `auth login --timeout` to `--login-timeout` (pre-release, the shadow of the global `--timeout` is a footgun) and mention it beside the Binary `-o` shadow note in the docs header; Q13 add one sentence that a Binary command's local `-o <file>` ignores `FRESHBOOKS_OUTPUT`; Q15 rewrite the exit-2 row as an open list ("usage errors, for example: ..."); Q16 fix the `runtimeError` comment in `cli/internal/cmd/errors.go`; Q17 the quickstart leads with `FRESHBOOKS_CLIENT_ID`/`FRESHBOOKS_CLIENT_SECRET` env vars and mentions the flags second; Q18 mark `--base-url` hidden in the env table; Q20 one positive marker assertion per HTTP-issuing redaction scenario (assert the debug log contains the request line) and drop the vacuous `FRESHBOOKS_CLIENT_SECRET` assertion from scenarios that never read it; Q21 decision: the `docsHeader` text is UNWRAPPED (it is the top of a shipped `docs/*.md`, so the house rule wins over "Go source wraps like Go source") -- reflow it to one paragraph per line as part of the D6 move; Q22 a one-line comment on each package-level test seam (`stdinIsTerminal`, `stdoutIsTerminal`, `testTransport`, `testAuthEndpoints`) stating it is safe only because no test in the package calls `t.Parallel()`. Regenerate `docs/cli.md` (`mise run docs`) in the same commit.
- **D10 -- changelogs take their 0.1.0 shape under `[Unreleased]`.** Each module changelog keeps the heading `## [Unreleased]` (the attended tag step renames it to `## [0.1.0] - <date>`), but its body becomes the section the release will ship: for a first release only `### Added` is meaningful, so rewrite each into a concise `### Added` list grouped by area (lib: client/transport, auth, types/errors/pagination, then one bullet per service family; mcp: tools, transports, config, security; cli: registry commands, auth, config/contexts, output, dry-run, exit codes, docs) and DROP the `### Changed`/`### Fixed` entries that describe pre-release churn (they live in git history and `docs/phases/*/`). Fold anything from those sections that describes shipped behaviour (redaction, rotation write-back, retry, `omitzero`) into the relevant Added bullet. Unwrap every line (backlog item 12: all three are hard-wrapped scaffold style). Acceptance: `scripts/changelog-section.sh <module> Unreleased` prints a non-empty section for each of `freshbooks`, `mcp`, `cli` (the script matches the bracketed text literally, so `Unreleased` works as the "version"), and `grep -c '^## \[' <module>/CHANGELOG.md` is `1`. Root `CHANGELOG.md` `[Unreleased]` gets a `### Changed` bullet for the release-flow rework (D1-D4, D8) and a `### Added` bullet for syft/docsgen; the lead adds the phase-ship line at merge.

## Fold list (one checkpoint commit each, in this order)

- **F1** `chore(deps): refresh module dependencies and toolchain pins` -- D5 (mise.toml pins first, then modules; run `mise install`).
- **F2** `chore(ci): pin actions by SHA and drop the artifact round trip` -- D4 in `ci.yml` + `release.yml` (the workflow shape only; D1's goreleaser step lands in F3).
- **F3** `fix(release): let goreleaser build and gh publish the prefixed tags` -- D1 + D2 + D3 (`mise.toml` syft, both `.goreleaser.yaml`, the `release` job's two steps, `docs/building.md` release-flow section rewritten to match).
- **F4** `refactor(cli): move cobra/doc generation behind a docsgen build tag` -- D6 (+ `scripts/docs.sh`, `scripts/check.sh` tag list). `docs/cli.md` diff must be empty.
- **F5** `feat(cli,mcp): fall back to the module version when no ldflags are set` -- D7.
- **F6** `chore(scripts): ignore phase reports in the dirty-tree guard` -- D8.
- **F7** `fix(cli): fold the Phase 4 QA advisories` -- D9 (regenerates `docs/cli.md`).
- **F8** `docs: bring the guides in line with the shipped lib, MCP, and CLI` -- the docs pass (below).
- **F9** `docs: shape the module changelogs for 0.1.0` -- D10.
- **F10** `docs(phase-5): record the goreleaser snapshot dry runs` -- run, from each of `mcp/` and `cli/`: `GORELEASER_CURRENT_TAG=v0.1.0 mise exec -- goreleaser release --snapshot --skip=publish --clean > /tmp/gr-<module>.log 2>&1; echo $?`, then inspect `dist/`: list the archive names (must start with `freshbooks-mcp_0.1.0` / `freshbooks_0.1.0`, six each, windows as zip), run the linux/amd64 binary's `version` (prints `<binary> 0.1.0-SNAPSHOT-<sha>` or the snapshot template's equivalent -- record the exact string), `sha256sum -c checksums.txt --ignore-missing` from `dist/`, and confirm one `.sbom.json` per archive parses as JSON (`python3 -c` or `jq .bomFormat`). Also run the non-snapshot form once in a scratch clone with a real local tag exactly as D1 describes (`git clone <repo root> /tmp/gr-clone && git -C /tmp/gr-clone tag mcp/v0.1.0`, `mise trust` it, run in `/tmp/gr-clone/mcp` with `--skip=publish,validate`) to prove the CI command line works against a prefixed tag; record its tail. Put all of it in the implementer report (F10's commit is the report only; `dist/` is gitignored).

## Docs pass (F8) -- what "accurate" means

Files: `README.md`, `docs/getting-started.md` (still the Phase 0 stub: write it for real -- register an app on the FreshBooks portal with an HTTPS redirect `https://localhost:8765/callback`, the scopes page, then one working first call each from the lib (`go get` + `auth.StaticTokenSource` or `NewTokenSource` + `client.Identity.Me`), the CLI (`auth login` with env vars, `identity me`, `config set-context`), and the MCP server (`freshbooks-mcp serve` stdio config for Claude Desktop/Claude Code, and the HTTP `curl` shape) linking to the deeper guides), `docs/building.md` (gate, vuln, D8 note, release flow per D1-D3, docs generation section is stale -- describe the `docsgen` tag and `mise run docs`), `docs/library.md`, `docs/authentication.md`, `docs/mcp.md`, and the `docs/cli.md` header (through `docsgen` only). Rules:

- Every command, subcommand, flag, env var, tool name, option name, and type name mentioned in a doc must exist in the code with that exact spelling. Verify mechanically, not by eye: extract backticked identifiers from each doc (`grep -o '`[^`]*`'`), and for each CLI command path run `mise exec -- go -C cli run ./cmd/freshbooks <path> --help > /dev/null`, for each `FRESHBOOKS_*` env name `grep -rn` it in `cli/ mcp/ freshbooks/` (non-test Go), for each lib identifier `grep -rn` it in `freshbooks/*.go`. List the dead references you fixed in the report.
- Every example command in a doc must run: `--help` at minimum for CLI commands; the MCP `tools` manifest example against the built binary; a lib snippet must compile (drop it into a scratch `main.go` under `/tmp` with a `replace` directive, or keep it as an `Example*` test).
- ASCII only (`grep -nP '[^\x00-\x7F]' README.md docs/*.md` prints nothing), no hard wraps in prose (one paragraph per line), `--` and `->` not em dashes or arrows, no operator-specific strings (run `scripts/redaction-check.sh` before every commit).
- README: the Install section stays honest -- binaries and `go install` work "once the v0.1.0 tags ship"; say that the release job publishes archives, `checksums.txt`, and CycloneDX/SPDX SBOMs per release, and how to verify a download with `sha256sum -c`. Keep the Status column `pre-release` (the tag step flips it).
- Do not restate the command reference in prose; link to `docs/cli.md` and pkg.go.dev.

## Gate

- `mise run check` green on a clean tree after every checkpoint commit that touches Go or scripts (per-module runs are fine between; the full gate before F10 and at the very end); `mise run inventory-check` still `implemented 213, todo 0`; `mise run vuln` clean; `mise run actionlint` green; coverage >= 90% in all three modules.
- Dependencies allowed: unchanged sets (lib stdlib + testify; mcp go-sdk + jsonschema-go + cobra; cli cobra + pflag + yaml.v3 + x/term). `cobra/doc` stays in `cli/go.mod` (tests and the tagged command need it) but must not be linked into the untagged binary (D6 acceptance).
- Conventional commits as listed; **stage and commit in separate Bash calls**; `scripts/redaction-check.sh` before each commit; never `--no-verify`. Do NOT push, do NOT merge, do NOT create tags.

## Gotchas

- Bash `cd` persists across your tool calls; prefer `-C <module>` and absolute paths.
- `mise exec -- go`, never bare `go` (toolchain skew on this machine).
- `go list -m all` / `go get` in a workspace rewrites `go.work.sum`; that is expected in F1 and must be committed there, and must NOT appear as a stray diff in any later commit.
- The snapshot dry run's `{{.Version}}` is `0.1.0-SNAPSHOT-<sha>` by default; that is fine for the dry run, do not "fix" it.
- A generated-surface diff is proof: capture `docs/cli.md` before F4 and diff after (must be empty), and `freshbooks-mcp tools > /tmp/tools-before.json` before F1 and diff at the end (must be empty; the dependency refresh must not change the manifest).
- Redirect URIs must be HTTPS; do not "simplify" any doc example to `http://localhost`.

## Reporting (both channels)

When done (full gate green, `git status --porcelain` empty): write the report to `docs/phases/5/reports/implementer.md` (commit it as F10), send the same report with `SendMessage` to `team-lead` (report in `message`, not `summary`), AND return it as your final text. Report: `git log --oneline main..phase-5/release`, the exact `mise run check` tail, coverage per module, the dependency diff (`git diff main -- '*/go.mod' go.work.sum | grep '^[-+]' | grep -v '^[-+][-+]'`), the D6 `go version -m` proof, the D7 test names, the F10 dry-run evidence, the dead doc references fixed, `git status --porcelain` output, and every place reality disagreed with D1-D10. If you are genuinely blocked, report the blocker the same way instead of guessing.

---

## Lead-owned (not the implementer's)

- Gate dispatch (code-review, simplify, security in parallel, then QA) per `GOAL.md` stage 3; the security lane's focus list; QA's four mandatory probes.
- Closing the five stale dependabot PRs (#1-#5) after the merge (`gh pr close`), since F2/F3 supersede them.
- Root `CHANGELOG.md` phase-ship line, `docs/progress.md` ledger + discoveries + the attended tag runbook (draft below), `GOAL.md` retarget.

## Release checklist (attended tag step -- draft for `docs/progress.md`)

Preconditions: `main` green on GitHub, `git status --porcelain` empty, `gh auth status` shows `repo` + `workflow`, `mise install` done.

1. **Lib.** Edit `freshbooks/CHANGELOG.md`: `## [Unreleased]` -> `## [0.1.0] - YYYY-MM-DD` and add a fresh empty `## [Unreleased]` above it; root `CHANGELOG.md` line. Commit `release(freshbooks): v0.1.0`, push `main`, wait for CI. `git tag -a freshbooks/v0.1.0 -m "freshbooks v0.1.0"`, `git push origin freshbooks/v0.1.0`, `gh run watch` (the Release workflow: guard -> ci -> release), `gh release view freshbooks/v0.1.0`. Proxy pickup: `cd /tmp && GOFLAGS=-mod=mod mise exec -- go list -m github.com/InfiniteRoomLabs/freshbooks-tools/freshbooks@v0.1.0`.
2. **Bump.** In `mcp/` and `cli/`: `mise exec -- go get github.com/InfiniteRoomLabs/freshbooks-tools/freshbooks@v0.1.0 && mise exec -- go mod tidy`; rename `mcp/CHANGELOG.md` and `cli/CHANGELOG.md` sections the same way; commit `release(mcp,cli): require freshbooks v0.1.0 and cut 0.1.0`, push, wait for CI.
3. **MCP.** `git tag -a mcp/v0.1.0 -m "freshbooks-mcp v0.1.0"`, push the tag, `gh run watch`, `gh release view mcp/v0.1.0` (six archives + `checksums.txt` + six `.sbom.json`), download one archive and `sha256sum -c checksums.txt --ignore-missing`, then `cd /tmp && mise exec -- go install github.com/InfiniteRoomLabs/freshbooks-tools/mcp/cmd/freshbooks-mcp@v0.1.0 && freshbooks-mcp version` prints `freshbooks-mcp v0.1.0`.
4. **CLI.** Same as 3 with `cli/v0.1.0`, `freshbooks version`.
5. **After.** README Status column -> released versions; `docs/progress.md` ledger; `GOAL.md` retarget to the live-conformance pass.
