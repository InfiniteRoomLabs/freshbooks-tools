# Work order: Phase 8 implementer (convergence from the captures)

Dispatch: `Agent(subagent_type: "general-purpose", model: "sonnet", name: "phase-8-impl")`. Unattended; no live calls by the implementer (the lead runs the live suite in QA).

Stage 1 (lead, 2026-09-03): `main` @ `fbc62c4` green, tree clean, `implemented 213, todo 0`. Decisions:

- **D1 -- time-entry totals.** `freshbooks/testdata/seed/time_entries/list.json` `meta` carries `total_logged` (int), `total_unbilled` (int), `total_logged_per_team_member` (list), `total_logged_per_client` (list) beside the four `PageMeta` keys. Add `TimeEntryTotals{TotalLogged int; TotalUnbilled int; PerTeamMember []TimeEntryTotalByMember; PerClient []TimeEntryTotalByClient}` and `TimeEntriesService.ListWithTotals(ctx, businessID, opts ...RequestOption) (*TimeEntriesPage, error)` where `TimeEntriesPage struct { Page[TimeEntry]; Totals TimeEntryTotals }`. One request; decode the body into one private envelope (`PageMeta` + `time_entries` + the four totals) and build the `Page` with `newPage`. `List` is untouched. The two list element shapes are empty on the captured account: take them from https://www.freshbooks.com/api/timetracking (the `time_entries` list response section); if the docs do not show them, keep those two fields as `json.RawMessage` with a comment saying so and a `STATE AS OF 2026-09-03 (Phase 8)` spec bullet in section 5.1 marking them INFERRED. `Page[T]` and `PageMeta` do not change.
- **D2 -- unmodelled keys.** Model every key present in `freshbooks/testdata/seed/expenses/list.json` `expenses[0]` and `freshbooks/testdata/seed/ledger_accounts/list.json` `data[0]` that has no `json` tag on `Expense` / `LedgerAccount` today (compute the exact list by diffing the capture's keys against the struct's tags, including camelCase tags such as `taxAmount1`; the lead's rough count is ~14 + 3). Types from the capture with `null` handled as pointers or `omitzero` value types matching the file's existing conventions; every new field gets `omitzero` so request bodies written today do not change. `Expense.Billable bool` and `Version string` are the ones a caller will use; `jea_id`/`jesa_id`/`category_id` on `LedgerAccount` as `*int64`/`*string` per the capture. Re-seed the unit fixtures `freshbooks/testdata/accounting/expenses_*.json` and `testdata/ledger_accounts/*.json` from the captures (synthetic ids only) and extend `TestLiveLedgerAccounts`/`TestLiveExpenses` (or add `TestLiveExpenseFields`) to assert the new fields decode from the capture shape; the lead runs them live in QA.
- **D3 -- redaction script.** `scripts/redaction-check.sh` gains `--range <base>..<head>` (reads `git diff <range> -U0` added lines instead of the staged index, same term logic and exit codes) and, in both modes, a 6+-digit integer sweep over every changed file under `freshbooks/testdata/seed/` with an allowlist regex for the known placeholders (`^700[0-9]{2}$`, `8675309`, `4242424`, `5555550100`, `999999999`, `1111111`, and all-zero runs); any other 6+-digit integer fails with the file and line. Keep the `#!/usr/bin/env -S usage bash` shebang and add the new flag as a `#USAGE flag`; `usage lint` must pass. A `scripts/redaction-check_test.sh`-style check is not required; verify by hand against `main..phase-8/converge` and record the output in the report.
- **D4 -- `usage` shebang stays** on every script (the operator's global convention; mise verifies no checksum for `aqua:jdx/usage` today -- recorded in `docs/progress.md` backlog item 9 as "keep; revisit when aqua adds one").
- **D5 -- additive only.** No exported signature changes, no wire-shape changes on existing fields, no new dependencies. `freshbooks/CHANGELOG.md` `[Unreleased]` `### Added` entries for D1 and D2; root `CHANGELOG.md` `[Unreleased]` `### Changed` line for D3. `docs/progress.md` backlog items 9, 12, 14 closed or re-cut; `docs/library.md` gains one sentence on `ListWithTotals`.

---

You are implementing **Phase 8** of `freshbooks-tools` in `<repo root>` on branch `phase-8/converge` (checked out, clean; this plan is its first commit). Read first: `CLAUDE.md`; `docs/progress.md` (backlog items 9, 12, 14); `docs/phases/7/reports/qa.md` Q3 and `security.md` A9; the three captures named above; `freshbooks/page.go`, `freshbooks/time_entries.go`, `freshbooks/expenses.go`, `freshbooks/ledger_accounts.go`, `freshbooks/live_conformance_test.go`, `scripts/redaction-check.sh`.

Rules: `mise exec -- go`, never bare `go`; tests first; `-race`; coverage floor 90% per module; stage and commit in separate Bash calls; `scripts/redaction-check.sh` before every commit; conventional commits (`feat(freshbooks): ...`, `chore(scripts): ...`, `docs: ...`); checkpoint commits per decision (D1, D2, D3, then docs); no push, merge, or tags; no live calls (you have no token; do not look for one; the `-tags live` tests must compile via `go vet -tags live ./freshbooks/` but are not run by you). Context budget: never cat large files; gate output to a log file, read the tail.

Report: `docs/phases/8/reports/implementer.md` (commit it), `SendMessage` to `team-lead` (full report in `message`), and the same as your final text: what D1/D2/D3 produced (the exact new fields), the redaction script's output on `--range main..phase-8/converge`, the gate tail with the three `coverage-gate:` lines, `git log --oneline main..phase-8/converge`, `git status --porcelain`, and anything where reality disagreed with D1-D5.
