# Progress

Living status doc. Read first, update at every phase boundary. Last updated: 2026-09-03 (Phase 8 shipped; freshbooks/v0.3.0, mcp/v0.1.2, cli/v0.1.2 released).

## Current state

- **Phase 8 (convergence from the captures) shipped 2026-09-03**, `phase-8/converge` -> `main` @ `911ba6b` (`--no-ff`), then `freshbooks/v0.3.0` (`1b9c8b3`, additive) and `mcp/v0.1.2` + `cli/v0.1.2` (`3c728c5`), every Release run green first time, `go install ...@v0.1.2` verified. Unattended: `TimeEntries.ListWithTotals` (+ the matching MCP tool and CLI command, surface now 169), the 17 unmodelled `Expense`/`LedgerAccount` keys typed from the captures, `scripts/redaction-check.sh --range` plus a 6+-digit integer sweep with a HEAD baseline in staged mode, and `scripts/redaction-selftest.sh` (19 probes) wired into the gate. Gate: code review 2 blocking (stale tool count, untagged `Totals`), security 1 blocking (the new range mode's term scan was inert: a basic-regex `\+`), 21-item fix in five checkpoints, QA NEEDS WORK on parity-doc line references only (lead-fixed) then everything else PASS incl. 857 live tests. Coverage at ship: freshbooks 91.9%, mcp 92.1%, cli 91.6%.
- **Phase 8 (convergence from the captures) implemented 2026-09-03**, branch `phase-8/converge`, not yet reviewed or merged. Unattended; no live calls. Three checkpoint commits: D1 `TimeEntriesService.ListWithTotals` (backlog item 12, closed), D2 the 17 unmodelled `Expense`/`LedgerAccount` keys (backlog item 14, closed), D3 `scripts/redaction-check.sh` gains `--range <base>..<head>` plus a 6+-digit seed-corpus sweep (Phase 7 security A9, QA Q4). D1 rippled into the MCP and CLI frozen tool/command surfaces (both parity-test every exported `freshbooks.Client` service method): 168 -> 169 throughout, one new keyless tool/command each (`time_entries_list_with_totals`, `time-entries list-with-totals`) wrapping the same wire endpoint as `time_entries_list`/`time-entries list`, not a new inventory key. D3 found two things the plan's text got wrong against the real repo: `scripts/redaction-check.sh` did not already carry the `usage` shebang the plan assumed (converted it, backlog item 9 closed), and the corpus carries both phone placeholders -- the plan named `5555550100` (`freshbooks/testdata/seed/staff/list.json`), the captures also carry `5550100100` (`freshbooks/testdata/seed/users_me.json:33`); both are allowlisted (Phase 8 code review R6). The sweep also needed a UUID-token exemption: a hyphen-segment of this repo's own `00000000-0000-4000-8000-000000000NNN` synthetic-uuid convention is itself a 6+-digit all-decimal run and was false-positiving before the fix. Verified clean against `main..phase-8/converge` and, by hand, against every file in `freshbooks/testdata/seed/`. Gate green (`mise run check`, dirty-tree banner only). Coverage: freshbooks 91.9%, mcp 92.1%, cli 91.6%.
- **Phase 7 (live conformance) shipped 2026-09-03**, `phase-7/live` -> `main` @ `0cd4e89` (`--no-ff`), followed by `freshbooks/v0.2.0` (`6bac43c`; breaking: the ledger-account taxonomy methods return typed values) and `mcp/v0.1.1` + `cli/v0.1.1` (`6715fda`), every Release run green first time, `go install ...@v0.1.1` verified for both binaries. Against IRL's real books (read-only; no sandbox, so the eight write facts stay deferred): nine read facts confirmed or corrected with captures under `freshbooks/testdata/seed/`, a `-tags live` conformance suite (9 tests, zero skips), six lib fixes (paginated expense vendors, unified Stripe gateway, typed ledger taxonomy, staff `identity_uuid`/`language`, `Page.Sort`, an empty vendor list encodes `[]`), and three CLI fixes found by the first real login (only grantable scopes by default -- three of the documented `:write` scopes do not exist; `auth token` refreshes an expired token; the loopback's TLS-handshake noise silenced). Gate: code review 1 blocking + security 1 blocking (the same six real ledger row ids in one capture, found independently) -> 14-item fix order in three checkpoints -> QA PASS first round. Coverage at ship: freshbooks 91.8%, mcp 92.1%, cli 91.6%.
- **Phase 7 (live conformance) implemented 2026-09-03**, branch `phase-7/live`, not yet reviewed or merged. Nine read facts run against IRL's real books with a 45-scope token (attended; no sandbox exists, so every write fact is DEFERRED -- see `docs/phases/7/reports/lead-sandbox.md`). Five wire disagreements found and fixed in the library (expense vendors, the unified Stripe gateway connection, the ledger taxonomy endpoints, staff `identity_uuid`/`language`, `meta.sort`), one shipped CLI bug found and fixed (`auth token` printed expired tokens), and one fact (business-family sort direction) proved unresolvable on this account and left alone. Captures under `freshbooks/testdata/seed/`, one `TestLive*` per fact in `freshbooks/live_conformance_test.go`. Coverage: freshbooks 91.8%, mcp 92.1%, cli 91.6%.
- **v0.1.0 shipped 2026-09-02** (attended, then finished on Wes's go-ahead in-session): `freshbooks/v0.1.0` @ `0b63858` (notes-only release, proxy serves `freshbooks@v0.1.0`), `mcp/v0.1.0` and `cli/v0.1.0` @ `90cc8cd` (13 assets each: six archives, `checksums.txt`, six SPDX 2.3 SBOMs; checksums verified on a download; `go install ...@v0.1.0` prints `freshbooks-mcp v0.1.0` and `v0.1.0`; the CLI binary links no `cobra/doc`). The release workflow worked first time on all three tags; the lib release shipped untitled (fixed by `gh release edit`, and the workflow now passes `--title`). Repository tag-protection ruleset (backlog item 10) was NOT applied: still open.
- **Phase 5 (release hardening) shipped 2026-09-02**, `phase-5/release` -> `main` @ `6cbe6e4` (`--no-ff`). The release workflow was broken as written and is now proven: goreleaser builds with `--skip=publish,validate` and `gh release create --verify-tag` publishes onto the real prefixed tag (goreleaser OSS validates `GORELEASER_CURRENT_TAG` against `git describe --exact-match` and would have created the GitHub release on a new `v0.1.0` tag); `project_name` per module (archives no longer collide as `freshbooks-tools_*`), `GOWORK=off` + `CGO_ENABLED=0` builds, syft (mise-pinned `aqua:anchore/syft`) SPDX 2.3 SBOMs. CI: `actions/checkout` v7.0.1 and `jdx/mise-action` v4.3.0 pinned by SHA, `upload-artifact`/`download-artifact`/`goreleaser-action` removed, `persist-credentials: false` on the write-scoped job, `failglob` before publishing. Toolchain goreleaser 2.18.0, golangci-lint 2.13.2; dependencies refreshed (x/sys 0.47.0 closes GO-2026-5024). Folds: `cobra/doc` behind the `docsgen` build tag (`cli/internal/docsgen`, drift test still untagged and in the default gate), `version` falls back to `debug.ReadBuildInfo` so `go install ...@<ref>` prints the module version, `scripts/check.sh` ignores `docs/phases/*/reports/*`, `auth login --timeout` renamed `--login-timeout`, Phase 4 QA advisories Q4/Q12/Q15/Q17/Q20/Q21/Q22 folded. Docs pass complete (`getting-started.md` written for real; every command/env/identifier verified mechanically). Module changelogs reshaped into their 0.1.0 `### Added` sections, unwrapped, still under `## [Unreleased]`. Coverage at ship: freshbooks 91.8%, mcp 92.1%, cli 91.5%.
- **Gate outcome:** code review REQUEST CHANGES (R1 blocking: docs claimed CycloneDX SBOMs, syft's goreleaser default is SPDX), security PASS (A1-A5 advisory), simplification 3 apply-recommended -> one lead-landed fix commit (`6e41003`, 11 edits) -> QA PASS on the first round (goreleaser snapshot + real-tag dry runs from a clean clone, `go install @sha` outside the workspace, every doc example run, gate green). Q1/Q2 doc advisories folded in `5e3108f`.
- **Phase 4 (CLI) shipped 2026-09-02** (`3ae853d`): 168 registry commands, loopback PKCE login, contexts, `json|yaml|table|name`, `--dry-run`, `api`, exit codes, generated `docs/cli.md`. **Phase 3 (MCP server) shipped 2026-09-01** (`7ed5891`): 168 tools over 212 keys. **Phase 2 (lib resources) shipped 2026-09-01**; inventory `implemented 213, todo 0`. **Phase 1 (lib core) shipped 2026-08-23** (`98ea08c`). **Phase 0 (scaffold) shipped 2026-08-22** (`b3063ba`).
- Spec callouts: section 7 `STATE AS OF 2026-09-02` (Phase 4), section 6 (Phase 3), sections 3/5.1 (Phase 2). Phase 5 added none: the release design in section 8.4 still describes the intent correctly; `docs/building.md` carries the mechanics. Phase 7 added `STATE AS OF 2026-09-03 (Phase 7, live)` callouts to sections 3 and 5.1. **Read-path facts for the resources named below are now live-confirmed; every write-path fact is still docs-confirmed only.**

## Phase ledger

| Phase | Status | Branch / merge | Notes |
|---|---|---|---|
| 0 Scaffold | **SHIPPED 2026-08-22** | `phase-0/scaffold` -> `main` @ `b3063ba` | reports in `docs/phases/0/` |
| 1 Lib core | **SHIPPED 2026-08-23** | `phase-1/lib-core` -> `main` @ `98ea08c` | one converged blocker (`Token.String`); reports in `docs/phases/1/` |
| 2a-2d Lib resources | **SHIPPED 2026-09-01** | `phase-2/{a,b,c,d}` merged sequentially | 213/213 keys; reports in `docs/phases/2/` |
| 3 MCP | **SHIPPED 2026-09-01** | `phase-3/mcp` -> `main` @ `7ed5891` | review 1 + security 1 blocking -> 1 fix commit -> QA PASS; reports in `docs/phases/3/` |
| 4 CLI | **SHIPPED 2026-09-02** | `phase-4/cli` -> `main` @ `3ae853d` | review 10 + security 3 blocking -> F1-F30 (checkpointed) -> QA NEEDS WORK x2 -> PASS; reports in `docs/phases/4/` |
| 5 Release hardening | **SHIPPED 2026-09-02** | `phase-5/release` -> `main` @ `6cbe6e4` | stage 1 found the release workflow could not ship a prefixed tag; review 1 blocking -> 1 lead-landed fix -> QA PASS first round; reports in `docs/phases/5/` |
| 6 v0.1.0 tags | **SHIPPED 2026-09-02** | `freshbooks/v0.1.0` @ `0b63858`, `mcp/v0.1.0` + `cli/v0.1.0` @ `90cc8cd` | Release workflow green on all three; assets, checksums, SBOMs, and `go install` verified |
| 7 Live conformance | **SHIPPED 2026-09-03** | `phase-7/live` -> `main` @ `0cd4e89` | attended; 9 read facts, 8 writes deferred (real books); review 1 + security 1 blocking -> 14-item fix -> QA PASS; reports in `docs/phases/7/` |
| 8 v0.2.0 / 0.1.1 releases | **SHIPPED 2026-09-03** | `freshbooks/v0.2.0` @ `6bac43c`, `mcp/v0.1.1` + `cli/v0.1.1` @ `6715fda` | Release runs green first time; lib minor bump for the typed ledger taxonomy |
| 9 Convergence | **SHIPPED 2026-09-03** | `phase-8/converge` -> `main` @ `911ba6b` | unattended; review 2 + security 1 blocking -> F1-F21 -> QA docs-only NEEDS WORK -> lead fix; reports in `docs/phases/8/` |
| 10 v0.3.0 / 0.1.2 releases | **SHIPPED 2026-09-03** | `freshbooks/v0.3.0` @ `1b9c8b3`, `mcp/v0.1.2` + `cli/v0.1.2` @ `3c728c5` | Release runs green first time; the third hand-driven release cycle in a day, which produced the release-automation PRD in the ideas repo |

## Discoveries (Phase 5)

- 2026-09-02: goreleaser OSS 2.18.0 has no `monorepo` config (Pro-only still). With `GORELEASER_CURRENT_TAG=v0.1.0` on a commit tagged `mcp/v0.1.0`, `validate()` runs `git describe --exact-match --match v0.1.0` and fails ("tag v0.1.0 was not made against commit"); and the GitHub release pipe creates the release on `ctx.Git.CurrentTag`, i.e. it would have minted a new `v0.1.0` tag. `--skip=publish,validate` plus `gh release create --verify-tag <real tag> dist/...` is the working shape (verified from source and in a scratch clone).
- 2026-09-02: goreleaser's `project_name` defaults to the git remote's repository name, so both modules produced `freshbooks-tools_0.1.0_*` archives until `project_name` was set per module. SBOM generation shells out to `syft`, which nothing had installed; the default SBOM format is SPDX 2.3 JSON, not CycloneDX.
- 2026-09-02: git 2.43's pathspec `:(exclude)docs/phases/*/reports` excludes nothing; the trailing `/*` form (`:(exclude)docs/phases/*/reports/*`) works for both untracked and modified files. A wholly new untracked phase directory whose only contents are reports collapses to one `??` entry and is excluded as a unit; once the phase has any tracked file (the plan), non-report strays inside it are reported normally.
- 2026-09-02: a plain `go build` reports `debug.BuildInfo.Main.Version` as `(devel)`, a `go install ...@<sha>` reports the pseudo-version, and a `go install ...@vX.Y.Z` reports `vX.Y.Z`; the `version` fallback keys on the `0.0.0-dev` ldflags placeholder so goreleaser builds are never second-guessed. `mise exec -- go` outside the repo uses the machine's global Go pin, not this repo's (`compile: version mismatch`); QA used the pinned toolchain by absolute path under `~/.local/share/mise/installs/go/`.
- 2026-09-02: `freshbooks version` prints the bare version; `freshbooks-mcp version` prints `freshbooks-mcp <version>`. Deliberate and documented nowhere as a contract; the runbook below asserts each binary's real output.

## Phase-close backlog (convergence + live conformance)

Cross-phase items deferred by triage. Items folded by Phase 5 were removed (cobra/doc linkage, the dirty-tree guard, the dependency refresh, the changelog reflow, Q4/Q12/Q15/Q17/Q20/Q21/Q22).

1. **Business-family sort direction** -- STILL OPEN after the live pass, and now with a known reason. `Sort()` emits `field_desc`; the docs and one Postman capture say `-field`. Live (2026-09-03) the API validates the parameter not at all: `GET /projects/business/{id}/projects` answers 200 for `-updated_at`, for `updated_at_desc`, **and** for `no_such_field_desc`, echoing each back verbatim in `meta.sort`. With zero projects and zero time entries on the authorized account, orderings cannot be compared either, so nothing distinguishes the spellings. Resolving it needs an account holding at least two projects (or two time entries) whose order can be compared under each spelling. The CLI `--sort` and MCP caveats stay as written until then.
2. **CLI test hardening leftovers** (Phase 4 QA Q8, Q9): `-o json` output asserted for every command; permission tests use literal modes.
3. **Dual-stack loopback binding** (Phase 4 security A7): the listener is on `127.0.0.1` while the redirect says `localhost`; binding `::1` too is the real fix.
4. **DONE 2026-09-03** -- `PageMeta` dropped `meta.sort`. `Page[T]` and `PageMeta` now carry `Sort []string` (omitted from JSON when empty). Superseded by the new item 12 below, which is the part of the same `meta` block still being dropped.
5. **DONE 2026-09-03** -- live-checked and decided. Two *member* keys were genuinely being lost (`identity_uuid`, `language`) and are now decoded. The seventeen sibling *business* fields the same payload carries stay unreturned on purpose (`List(businessID)` is the staff listing; the business record belongs to `SettingsService` or `(*Client).Do`), and the reasoning is now written next to `staffListResponse`.
6. **Full-fixture sweep**: one verbatim captured-response fixture per resource; most fixtures are still trimmed.
7. **Live-conformance pass** -- read facts DONE 2026-09-03, write facts DEFERRED. The authorized account is IRL's production books; no sandbox exists, so nothing was created, updated, or deleted (`docs/phases/7/reports/lead-sandbox.md`). Still open, each needing an account it is safe to write to:
   - invoice delete verb (PUT vs DELETE-with-body), `Expenses.Delete`'s `vis_state` (1 vs the Postman 0), `Expenses/Create Custom Expense Category` (docs say unsupported), `Contacts.Update` + `Expenses.CreateRecurring` response envelopes, `Invoices.EnablePaymentOptions` body and its quoted-`entity_id` echo, webhook verify/resend `callback_id` bodies, checkout-link create/update shapes, quoted-ID writes.
   - `/uploads/` envelope family: confirming it means uploading a file.
   - PKCE enforcement with a deliberately wrong verifier (needs a second consent).
   - Tokenization shapes on `paid.freshbooks.com` (needs a test card and a connected gateway).
   - The `DateTime` zoneless `YYYY-MM-DDTHH:MM:SS` layout: no live producer exists on this account, because projects and time entries -- the only endpoints whose examples show it -- are both empty. Needs an account with either.

8. **govulncheck locally**: not installed as a tool; `scripts/check.sh` runs it via `go run ...@v1.7.0`, which is pinned and sufficient. Consider a mise pin only if the `go run` fetch becomes a CI cost.
9. **DONE 2026-09-03 (Phase 8, decided)** -- `usage` is the one mise tool with no checksum or signature (Phase 5 security A3) and it interprets `scripts/changelog-section.sh` (and, as of Phase 8, `scripts/redaction-check.sh`, which the D3 decision converted from a plain `#!/usr/bin/env bash` script to the `usage` wrapper so its new `--range` flag could be a real `#USAGE flag` -- the plan's D3 text assumed the wrapper was already there; it was not) in the release/gate paths. Decision: keep the `usage` shebang on every script that has it, including the newly-converted one, per the operator's global convention; revisit pinning it verifiably once aqua grows a checksum for `aqua:jdx/usage`.
10. **Repository tag-protection ruleset** (Phase 5 security A1): no ruleset guards `refs/tags/{freshbooks,mcp,cli}/v*`, and `enforce_admins` is off on `main`, so the release guard's "on main" premise is weaker than it reads. Pre-flight step 0 of the tag runbook below.
11. **`docsgen_test.go` could table-drive its five `strings.Contains` checks** (Phase 5 simplify S4). Cosmetic; do it the next time that file is touched.
12. **DONE 2026-09-03 (Phase 8)** -- time-entry `meta` totals were dropped (found live 2026-09-03). `TimeEntriesService.ListWithTotals` now runs the same one request as `List` and returns them via a new `TimeEntriesPage{Page[TimeEntry]; Totals TimeEntryTotals}`; `List` is untouched. `TimeEntryTotals.PerTeamMember`/`.PerClient` stay `json.RawMessage`: the captured account has zero time entries, so both are always `[]`, and neither the Postman collection nor the FreshBooks docs show a populated example -- re-cut as a new backlog item (15) needing an account with real time entries.
13. **Nine read captures exist, most resources still have none** (narrower restatement of item 6). `freshbooks/testdata/seed/` now holds live captures for callbacks, expenses, gateways, ledger accounts, projects, staff, and time entries. Everything else is still Postman-derived or trimmed.
14. **DONE 2026-09-03 (Phase 8)** -- unmodelled wire keys on two Phase 2 read models (QA Q3, live captures 2026-09-03): `Expense` was dropping 14 keys (`accountid`, `accounting_systemid`, `account_name`, `background_jobid`, `bank_name`, `billable`, `bill_matches`, `converse_projectid`, `ext_accountid`, `ext_invoiceid`, `ext_systemid`, `modern_projectid`, `potential_bill_payment`, `version`; `billable` was the one a caller would miss), `LedgerAccount` was dropping `category_id`, `jea_id`, `jesa_id`. All 17 now have struct tags, typed from the captures (pointer where the capture shows `null` -- INFERRED; value+`omitempty` where it shows a present zero/empty value). `LedgerAccount.CategoryID` has no non-null evidence at all in the capture, so its `*int64` typing (matching the codebase's other `*_id` fields) is a guess -- re-cut as backlog item 16 for whoever next sees a populated value.
15. **Time-entry totals breakdown-list element shape unknown** (split out of item 12 on its resolution, 2026-09-03). `TimeEntryTotals.PerTeamMember` and `.PerClient` are `json.RawMessage`: the captured account has zero time entries, and neither the Postman collection nor https://www.freshbooks.com/api/time_tracking shows a populated example of either list. Needs an account with real time entries to type them.
16. **`LedgerAccount.CategoryID`'s non-null shape is unconfirmed** (split out of item 14 on its resolution, 2026-09-03). Every account in the one captured business has `category_id: null`; typed `*int64` on the strength of this codebase's other `*_id` fields, not any positive evidence. Needs an account with a populated `category_id` to confirm or correct.
17. **Parity-doc line references and redaction findings** (Phase 8 QA Q3/Q5/Q6): `docs/phases/3/tools.md` and `docs/phases/4/commands.md` carry `(file.go:N)` references that no test checks, and 26 of 169 rows had drifted by the end of Phase 8 (12 corrected by hand); add a line-reference assertion to both parity parsers. Also: `Expense.AccountName`/`BankName` are empty in every fixture and asserted nowhere; `redaction-check.sh` term findings name the file but not the line although the extractor has it.

## v0.1.0 tag step (done 2026-09-02)

Ran as designed: lib changelog cut (`0b63858`) -> `freshbooks/v0.1.0` -> `go.mod` bump + mcp/cli changelog cuts + `--title` on `gh release create` (`90cc8cd`) -> `mcp/v0.1.0` -> `cli/v0.1.0`. Each Release run went guard -> ci -> release green. Still open from the pre-flight: the tag-protection ruleset (backlog item 10). The three "once the tags ship" doc caveats were flipped in the ship commit.

## Next action: Phase 9 (Docusaurus docs site, unattended)

Run `/goal complete everything in @GOAL.md` in a fresh session. Branch `phase-9/docs-site`, sonnet implementer, opus reviewers, pnpm only. Before it, if Wes wants the release toil gone first: the release-automation PRD lives in the ideas repo (`prds/prd-freshbooks-release-automation.md`); its layer 1 (`scripts/release.sh`, `mise run release`) is a half-day phase that could run before the docs site. The repository tag ruleset (backlog item 10) stays attended.

## Discoveries (Phase 7)

- 2026-09-03: **`freshbooks auth token` printed expired tokens.** Without `--refresh` it loaded the stored credentials and printed the access token verbatim, exit 0, even when `auth status` said `valid: false` in the same second. The documented `TOKEN=$(freshbooks auth token)` idiom therefore handed callers a credential that 401s on first use; only an actual API call (`identity me`) refreshed it. Fixed: `Token` refreshes through the store when the stored token is expired or inside the lib's `DefaultExpirySkew`.
- 2026-09-03: **the live API does not validate `sort` at all.** `sort=no_such_field_desc` answers 200 and is echoed back verbatim in `meta.sort`, exactly like a well-formed value. A parameter echo is not a confirmation, and no probe of an empty collection can distinguish two sort spellings.
- 2026-09-03: **the business family's filter encoding is provable through the validator, not through results.** With zero matching records, `updated_since=notadate` answering 422 (naming the field) while `search[updated_since]=notadate` answers 200 is the whole proof: only the spelling the server parses can fail validation.
- 2026-09-03: **an account onboarded through FreshBooks Payments answers `stripe_unified`, not `stripe`.** The Postman collection has no example of it, so `GatewayConnection` decoded a fully connected live account as three nil pointers -- a silent, total data loss that no unit test built from the collection could have caught.
- 2026-09-03: **`(*Client).Do` peels the envelope, so it cannot capture a verbatim body.** Confirming an envelope shape means a raw `http.Client` request; the seed captures in this phase were taken that way.

## How to resume in a fresh session

1. Read this file, then `GOAL.md`, then `CLAUDE.md`.
2. `git status --porcelain` must be empty and `git log --oneline -5` should match the ledger above. If not, reconcile before starting.
3. Read only the spec sections the current phase names.
4. Start the goal.
