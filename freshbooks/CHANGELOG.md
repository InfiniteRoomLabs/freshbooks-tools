# Changelog

All notable changes to the `freshbooks` module are documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/), and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Added

- `Client` with all 36 resource services declared as fields, `NewClient` and the `With*` options (`WithTokenSource`, `WithHTTPClient`, `WithBaseURL`, `WithUserAgent`, `WithLogger`, `WithRetry`, `WithClock`), and `(*Client).Do` as the escape hatch for unmodelled endpoints.
- Transport: a single `do()` path with family-aware query encoding, accounting and auth envelope unwrapping, family-specific error decoding, jittered exponential backoff on 429/502/503/504 honouring `Retry-After`, context cancellation during backoff, a 10MB response cap, and `Authorization` stripped on cross-host redirects (the redirect cap itself returns a real error instead of handing back the final 3xx as a response); `doMultipart` for the `/uploads/` endpoints (multipart/form-data, 10MB bound), `doOnHost` for FreshBooks' card-tokenization host (`paid.freshbooks.com`, distinct from the API base URL), `doRaw` for endpoints that answer a file instead of JSON, `softDelete` for the accounting family's vis_state-PUT soft delete, and a `pathSegment`/`noTraversal` guard rejecting a caller-supplied path segment carrying a slash, `?`, `#`, or a `.`/`..` traversal before it reaches the request path.
- `freshbooks/auth`: the PKCE authorization-code flow (`AuthCodeURL`, `Exchange`, `Refresh`, `Revoke`) against both live-verified endpoint sets, `Token` (redacts every secret through `fmt.Stringer`, including on a struct that embeds one) / `TokenSource` / `StaticTokenSource`, `NewTokenSource` with expiry skew, single-flight refresh, and rotation write-back before return (a `TokenStore.Save` failure after a successful refresh keeps the rotated pair in memory and retries the save on the next call, rather than discarding it or forcing re-authentication), plus `FileStore` (0600 in a 0700 directory, temp file plus rename) and `MemoryStore`. No fallback to `http.DefaultClient`: the package's own default has a 30s timeout and refuses to follow redirects, which would otherwise replay the client secret and refresh token to a redirect target.
- Types: `AccountID`, `BusinessID`, `BusinessUUID`, `Money` with `Rat()`, `Date` and `DateTime` covering all four FreshBooks wire formats (RFC 3339, `YYYY-MM-DD HH:MM:SS`, a bare date, and a zoneless `YYYY-MM-DDTHH:MM:SS` seen in the Projects/Time Tracking examples) and remembering which one it decoded from so a round trip preserves that family's spelling, `VisState`, and the `Include` / `Search` / `Sort` / `PageNumber` / `PerPage` request options. `Page[T]` (see Pagination below) and `identity.go`'s `User`/`Membership` carry `json` tags, so marshaling either (the MCP server and the CLI's table/json/yaml output both do) produces the documented snake_case wire shape.
- Errors: `*Error` with `errors.Is` sentinels (`ErrUnauthorized`, `ErrForbidden`, `ErrNotFound`, `ErrValidation`, `ErrRateLimited`), `RetryAfter()`, and a `Family` that reflects the request as built rather than one re-derived from the (possibly `WithBaseURL`-prefixed) path.
- Pagination: `Page[T]`, `PageMeta`, and the `All` iterator (`iter.Seq2`) over every paginated list.
- `IdentityService`: `Me`, `Whoami`, `Register`, `AddBusiness`, `DeleteBusiness`, `DeleteBusinessSubscription`, `ProvisionPayments`, `CreateApplication`, `Applications`, `UpdateApplication` -- the four `Authorization` inventory entries plus the Settings/Businesses and Settings/Developer keys, together with `auth.Config.Revoke`.
- `InvoicesService` (`List`, `All`, `Get`, `Create`, `Update`, `Delete`, `Send`, `PDF`, `ShareLink`, `EnablePaymentOptions`, `InvoicePresentationDefaults`), `InvoiceProfilesService` (recurring invoice templates), `ItemsService` (catalogue items), `PaymentsService` (invoice payments plus FreshBooks Payments checkout links: `CreateCheckoutLink`, `UpdateCheckoutLink`, `DeleteCheckoutLink`, `UpdateCheckoutLinkGateway`), and `RetainersService` (business-scoped recurring minimum-fee arrangements).
- `ClientsService` and `ContactsService` covering all 13 `Clients/*` inventory keys, with the client resource named `Customer` in Go to avoid colliding with the library's own `*Client` type.
- `CreditNotesService` (`List`, `Create`, `Update`, `Delete`) with `CreditType` distinguishing goodwill, prepayment, and overpayment credits.
- `EstimatesService` (`List`, `Get`, `Create`, `Update`, `Delete`, `Accept`, `Send`).
- `ExpensesService` (`List`, `Get`, `Create`, `Update`, `Delete`, `Summaries`, `Vendors`, `CreateRecurring`) and `ExpenseCategoriesService` (`List`, `Get`, `Create`).
- `TaxesService` (`List`, `Get`, `Create`, `Update`, `Delete`) covering the tax-rate operations duplicated across `Expenses/*`, `Accounting/Taxes/*`, and `Settings/Items and Services/*` as one method per operation with every inventory key stacked; `Delete` is a real HTTP `DELETE`, unlike every other soft-deleting resource.
- `BillsService` (`List`, `Create`, `Archive`, `Delete`), `BillPaymentsService` (`Create`, `Update`), and `BillVendorsService` (`List`, `Create`, `Update`, `Delete`).
- `LedgerAccountsService` (chart of accounts + type taxonomy, `BusinessUUID`-scoped), `JournalEntriesService` (`Create`, `Details`), `JournalEntryAccountsService.List`, and `OtherIncomeService` (`Create`, `List`, `Update`, `Delete`).
- `ReportsService`: all 13 accounting reports plus the business-scoped `TimeEntryDetails`; a report with no Postman example and no matching public docs page returns `json.RawMessage` rather than a guessed struct.
- `CallbacksService`: webhook subscriptions (`Register`, `List`, `Delete`, `Verify`, `ResendVerification`); webhook callback paths are classified as the accounting family so their envelope is unwrapped.
- `AttachmentsService.UploadExpenseReceipt` and `ImagesService` (`Upload`, `UploadWithoutAccount`).
- `GatewaysService.Get` and `PaymentOptionsService` (`FBPayTokenize`, `StripeTokenize`, `StripeCreateSetupIntent`, `SaveCreditCard`).
- `ProjectsService` (projects, abilities, discussion threads and comments), `TasksService`, `TimeEntriesService` (including the free-text `Search` endpoint), `ServicesService` and `ServiceRatesService` (spanning the business-family service catalogue and the accounting-family billable-items resource the same folder name conflates), `TeamMembersService` (team members, invitation and per-identity rates, invites), `StaffService` (the deprecated Staff resource plus the auth-family business-group member list), and `SystemsService.Get`.
- Ledger-account paths (`/accounting/businesses/.../ledger_accounts/...`, `/accounting/ledger_accounts/{types,sub_types}`) are classified as the business (flat, no envelope) family rather than the general accounting-enveloped case, so their actual `{"data": ...}` bodies decode correctly.
- All 213 FreshBooks Postman-collection inventory keys implemented across 36 services (`freshbooks/internal/inventory` is the parity-checking tool and normalized inventory).
