# Generated for Phase 0 from testdata/inventory.json.
# `ignore` is reserved for entries confirmed dead or a hard-coded-ID
# duplicate of another request, with evidence in the reason. The three
# my.freshbooks.com-hosted entries below are NOT confirmed dead -- they
# are the only create-journal-entry, delete-project, and
# delete-subscription operations in the collection, so they are todo
# until Phase 2 verifies the public host live and either implements
# them or demotes them to ignore with evidence.
# Never delete an ignore or todo entry; only move it between the two.
