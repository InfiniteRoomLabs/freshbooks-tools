// Package config will hold freshbooks-mcp's configuration: cobra flags with
// FRESHBOOKS_MCP_* environment twins (flag takes precedence over env), and
// no config file. Phase 3 fills this in; see docs/mcp.md.
package config
